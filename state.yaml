project: Hemlock
repository: https://github.com/deepgrove-ai/mlx-lm-deepgrove.git
branch: main
commit: eba96c16158f032821b0bf374ea1421cfddef0a9
workspace:
  folder: /Users/ianzvirbulis/Code/Hemlock
  synced_source: /Users/ianzvirbulis/Documents/ChatGPT/Hemlock
  migrated_from: /Users/ianzvirbulis/Documents/Codex/2026-08-13/https-github-com-deepgrove-ai-mlx/mlx-lm-deepgrove
setup:
  status: local_worktree_relocated
  environment: /Users/ianzvirbulis/Models/Hemlock/runtime
  python: "3.13 system-site-packages with local Transformers 5.7.0"
  model: deepgrove/maple-2bit-mlx
  model_directory: /Users/ianzvirbulis/Models/Hemlock/maple-2bit-mlx
  model_manifest: "16 files verified with hf cache verify"
  storage_policy:
    active_worktree: /Users/ianzvirbulis/Code/Hemlock
    lfs_concurrent_transfers: 1
    lfs_recent_remote_hydration: disabled
    lfs_objects: cleared_from_synced_source
    lfs_tmp: cleared_from_synced_source
    dream_min_free_bytes: 10737418240
  backup_environment: .venv-moved-backup-20260814
  simulator_cleanup:
    deleted_devices: true
    deleted_runtimes: ["iOS 18.0", "iOS 18.5"]
    retained_runtime: "iOS 26.5"
verification:
  repository: migrated_into_project_folder
  environment: fresh_venv_installed
  model: complete
  smoke_test: complete
  kernel_tests: "3 passed existing durable-kernel suite"
  strong_agent_tests: "32 passed via npm run test:agent"
  strong_agent_build: "npm run verify:agent passed"
  tool_use_benchmark: "11 fixed tasks under dream-chat/evals/tool-use/benchmark.json"
  tool_use_baseline: "empty-trace fixture baseline: 0% completion, 0 forbidden commands; not a Maple quality score"
  strong_agent_memory_records: ["mem_86860145079c433", "mem_9dba0ce54fbb4843", "mem_0682d6f371414a99"]
  generation: "24 tokens generated; peak memory 6.680 GB"
  lora_training_smoke: passed
  relocated_lora_training_smoke: passed
  relocated_lora_training_receipt: dream-runs/storage-relocated-training-smoke-verified-20260814/training-receipt.json
  relocated_lora_optimizer_steps: 1
  relocated_lora_train_loss: 4.891
  relocated_lora_validation_loss: 4.914
  relocated_lora_trainable_parameters: "11.608M/20214.030M (0.057%)"
  relocated_lora_adapter_bytes: 46433213
  relocated_lora_base_digest_unchanged: true
  agent_loop_lora_training_smoke: passed
  agent_loop_lora_training_receipt: "~/Library/Application Support/Hemlock/workspace-runtime/dream-runs/agent-loop-training-smoke-20260814/training-receipt.json"
  agent_loop_lora_optimizer_steps: 1
  agent_loop_lora_train_loss: 7.457
  agent_loop_lora_validation_loss: 7.121
  agent_loop_lora_adapter_bytes: 46433213
  agent_loop_lora_base_digest_unchanged: true
  agent_loop_training_worker_environment: "-S child trainer now receives venv and configured system site-packages, including MLX"
  strong_agent_tool_use_dream_smoke: passed
  strong_agent_tool_use_dream_receipt: "~/Library/Application Support/Hemlock/workspace-runtime/dream-runs/strong-agent-tool-use-smoke-20260814/training-receipt.json"
  strong_agent_tool_use_dream_metrics: "1 optimizer step; train loss 6.882; validation loss 6.502; 11.608M trainable parameters"
  strong_agent_tool_use_dream_base_digest_unchanged: true
models:
  launcher:
    prod_flow: "double-click / --app now runs electron directly against dist/ with HEMLOCK_PROD_UI=1 — no Vite dev server, no concurrently (saves ~7GB RAM, no terminal window)"
    dev_flow: "scripts/launch-hemlock.zsh --dev restores old Vite+HMR flow"
    rebuild_ui: "cd dream-chat && npm run build (required once before prod flow; dist/ ships prebuilt)"
  maple_2bit:
    directory: /Users/ianzvirbulis/Models/Hemlock/maple-2bit-mlx
    role: default local chat model ("default_model")
  lfm25_8b_a1b:
    repo: LiquidAI/LFM2.5-8B-A1B-MLX-4bit
    directory: /Users/ianzvirbulis/Models/Hemlock/LFM2.5-8B-A1B-mlx-4bit
    model_type: lfm2_moe (native mlx-lm support)
    params: "8B total, ~1.5B active (24 layers, 32 experts, 4 active, 2 dense)"
    dtype: 4-bit group-64 (official LiquidAI MLX quant, 4.5 GB)
    size_gb: 4.5
    role: selectable local model in Hemlock picker (maple lane, option "lfm25-8b")
    picker_value: lfm25-8b
    request_routing: main.cjs resolveLocalModelPath() maps picker value -> model dir; mlx_lm server lazy-loads per request
    smoke_test: "server on :8099 healthy; chat completion returned reasoning channel + 'LFM2.5 is alive' (finish=stop, 139 tokens)"
    bf16_note: "bf16 original (16.9GB) deleted - exceeded 16GB Mac GPU wired limit (GPU timeout) and filled disk"
  lfm25_dspark_draft:
    repo: LiquidAI/LFM2.5-8B-A1B-DSpark
    directory: /Users/ianzvirbulis/Models/Hemlock/LFM2.5-8B-A1B-DSpark-mlx
    model_type: dspark (custom mlx_lm/models/dspark.py wrapper; MODEL_REMAPPING lfm2_dspark->dspark)
    params: 327.7M (matches LiquidAI blog spec exactly)
    architecture: "DFlash-style 5-layer attention-only backbone fed by fc(10240->2048) on concat hidden states from target layers [2,6,10,14,18]; rank-256 factorized Markov head; confidence head on 2304->1"
    verified: "loads via mlx_lm.load_model; forward + draft heads produce (B,L,128000) logits and (B,L) confidence"
    limitation: "mlx-lm speculative_generate_step drives draft models with token ids; DSpark consumes target hidden states, so stock mlx-lm speculative decoding cannot use it. Day-one runtimes are llama.cpp + SGLang. On-device (M-series) speedup for the 8B MoE is ~18% per LiquidAI benchmarks."
  strong_agent_tool_use_dream_candidate_status: training_completed_candidate_pending
  strong_agent_candidate_inference_probe: blocked
  strong_agent_candidate_inference_reason: "The trained adapter server was healthy, but the structured-action probes did not yield a valid hemlock.agent.action.v1 envelope: the first spent its completion on reasoning/prose and hit the token limit; a no-thinking retry returned an invented list_directory action. No candidate quality or activation claim made."
  hemlock_sips_dataset_smoke: passed
  transformers_pin: "5.7.0 (avoids slow 5.x import scan on this Mac)"
server:
  status: passed_after_local_relocation
  pid: null
  host: 127.0.0.1
  port: 8080
  model: /Users/ianzvirbulis/Models/Hemlock/maple-2bit-mlx
  trust_remote_code: true
  flash_head: true
  defaults:
    temperature: 0.7
    top_p: 0.95
    top_k: 20
    max_tokens: 512
  health:
    models_endpoint: not_asserted
    chat_completions: passed_with_usage_telemetry
  note: Maple chat template emits reasoning in a separate reasoning field; final content is returned normally.
gui:
  status: hemlock_os_shell_and_agent_spine_built
  directory: dream-chat
  stack: React + Vite + Electron
  ui_overhaul:
    status: agent_cockpit_v2_implemented
    default_surface: "AI-first cockpit with workstream rail, lifecycle workbench, context/evidence ledger, command console, event spine, Dream/SIPS panel, heartbeat, and dock"
    concept_reference: "/Users/ianzvirbulis/.codex/generated_images/019ffefe-8478-7dc3-be68-9b66a12548ca/exec-596a0ed2-5d56-4017-ac7e-b311f24e2331.png"
    desktop_viewport: "1240x820 checked in Electron; cockpit readable with real event/context projections"
    narrow_viewport: "760x800 checked in Electron; workstreams and secondary surfaces stack without page-level horizontal overflow"
    visual_repairs: "removed raster logo card from dark chrome; added deterministic narrow stacking at <=800px"
  browser_url: http://127.0.0.1:5173/
  desktop_title: Hemlock · local dream
  local_api: http://127.0.0.1:8080
  build: passed
  desktop_render: passed
  sips_desktop_bridge: passed
  sips_status_command: passed
  sips_repo_map_command: passed
  sips_selfloop_start_complete: passed
  real_local_chat: passed_after_local_relocation
  thinking_display: "Maple-emitted reasoning/work channels are preserved and visible as Maple output; no host channel suppression is configured"
  dream_overlay: passed
  responsive_mobile_layout: passed
  facts_storage: browser_local_storage
  dream_behavior: real_local_lora_adapter
  dream_training_entrypoint: dream-chat/electron/dream_train.py
  dream_server_lifecycle: stop_train_restart_wait_ready
  dream_adapter_serving: per_request_adapters
  dream_live_debug: stage_elapsed_log_heartbeat
  dream_training_smoke: passed
  dream_training_smoke_receipt: dream-runs/storage-relocated-training-smoke-verified-20260814/training-receipt.json
  dream_candidate_inference: passed_with_usage_telemetry
  sips_control_center: implemented
  sips_control_runtime: dream-chat/electron/sips_runtime.py
  sips_routes: "status, repo-map, verify, recall, selfloop, cycle"
  sips_cycle: "baseline -> dataset -> Dream LoRA -> base/candidate comparison -> verification -> candidate receipt"
  sips_receipts: "~/Library/Application Support/Hemlock/workspace-runtime/sips-runs/"
  hemlock_os_shell: "system bar -> desktop canvas -> internal windows -> Understory dock -> command palette"
  hemlock_logo: "dedicated full Eastern Hemlock tree mark with trunk, roots, and layered boughs; leaf remains a semantic secondary icon"
  internal_window_manager: "open, close, focus, drag, resize, minimize, maximize, restore, snap, persisted geometry"
  os_surfaces: "Command Center, Chat/Code, SIPS Control, Memory Garden, Dream Lab, Activity, Receipts, Project Map, Settings"
  command_palette: "Cmd/Ctrl-K plus visible system-bar and dock entry; allowlisted commands only"
  agent_event_contract: hemlock.agent.event.v1
  task_state_contract: hemlock.agent.task.v1
  window_state_contract: hemlock.window.state.v1
  agent_event_spine: "Electron-owned append-only JSONL events with bounded replay projection and renderer subscription"
  session_recovery: "last durable session events are replayed; unfinished running/blocked task resumes as blocked for explicit inspection"
  lifecycle_events: "session, prompt, inference, command, verification, Dream, dataset, SIPS cycle, memory, closeout"
  capability_registry: "read, verify, train, memory commands with auto-run metadata, explicit training gates, and cancellation"
  agent_bridge: "mapleDesktop.agent and hemlockAgent compatibility surfaces; existing Dream/SIPS wrappers preserved"
  memory_policy: "candidate-first project lessons; explicit evidence-backed promotion gate; verify-before-use; demote/rollback append transition receipts"
  memory_transition_smoke: "candidate -> demote transition passed with relation target preserved"
  native_event_smoke: "session.started and session.closed receipts verified after controlled Electron launch"
  desktop_ui_smoke: "palette filtering, dock surface routing, minimize/restore, maximize/restore, title-bar drag verified"
  responsive_ui_smoke: "640x800 stack verified with main composer visible and no page-level horizontal overflow"
  sips_browser_boundary: rendered_only_no_local_commands
  sips_runtime_smoke: passed
  context_broker: dream-chat/electron/context_broker.cjs
  context_refresh_smoke: "fresh Computer History + available OpenChronicle; 13 redacted observations; source coverage 1.0"
  context_command_registry: "context.refresh and context.search through Electron agent:command"
  agent_kernel: "Electron-owned durable projection, task/operation queue, budgets, cancellation, replay journal, source policies, candidates, episodes, and training state"
  agent_projection: "hemlock.agent.projection.v1 persisted per repository workspace under Hemlock application data"
  personal_context: "Computer History, local project state, and OpenChronicle are explicit source lanes; notes, calendar, and mail remain disabled until user opt-in"
  ambient_inbox: "observations become provenance-bearing candidates; accept, dismiss, snooze, correct, and convert are separate transitions"
  coding_operator_loop: "intent -> recall -> inspect -> prepare change set -> verify -> approval -> apply -> verify -> remember"
  change_set_boundary: "prepared patches are isolated and approval-gated; source mutation is never automatic"
  runtime_storage_root: "~/Library/Application Support/Hemlock/{models,adapters,datasets,receipts,events,context,caches,workspaces}"
  runtime_storage_migration: "Dream/SIPS runtime defaults outside the Git worktree; legacy sips-runs is copied forward without destructive deletion"
  runtime_health: "process_exists, server_ready, inference_ready, training_ready, and storage_ready are tracked separately"
  agent_kernel_tests: "3 passed: projection persistence, budgets, malformed structured-context repair"
  agent_contracts: "hemlock.agent.action.v1, hemlock.agent.observation.v1, hemlock.agent.plan.v1"
  agent_orchestrator: "Electron-owned plan approval -> action validation -> registered command -> compact observation -> retry/block/complete"
  agent_budgets: "maxAgentSteps 8, maxCommands 12, maxRetriesPerOperation 2, maxMutationSets 1, maxTrainingCycles 0, maxWallClockMs 600000"
  agent_action_loop_tests: "32 focused contract, orchestrator, queue, context, artifact, preview, stream, and evaluation tests; all passed"
  agent_queue: "FIFO intent queue is durable and serial; steering bypasses the queue; queued cancellation leaves the active task untouched"
  agent_conversation_smoke: "Live Electron + Maple smoke passed: hey how are ya? produced a normal local response with durable task, queue, recall, inference, and completion events"
  agent_steering_smoke: "Live steering receipt passed; steering is recorded for the next bounded decision and does not rewrite an inference already in flight"
  agent_chat_trace: "Chat/Code keeps Maple model output primary: every emitted string channel is labeled and visible by default, while host telemetry, structured action envelopes, raw-output references, repairs, fallbacks, observations, and receipts remain separate"
  model_output_fidelity:
    stream_contract: "hemlock.agent.stream.v1 model_text frames preserve channel names, ordered sequence, delta, terminal, usage, and stop reason"
    suppression_removed: "enable_thinking:false removed from ordinary and structured-action inference bodies"
    durable_raw_output: "raw SSE/buffered payloads and channel digests persist under ~/Library/Application Support/Hemlock/events/model-output/"
    conversation_response: "hemlock.agent.conversation.response.v1 includes channels, rawOutputRef, traceRefs, and displayMode=model-verbatim"
    structured_action: "validated actions retain rawModelOutputRef, modelChannels, parseStatus, and fallbackMode without replacing raw Maple prose"
    adaptive_budgets: "320 tokens for short greetings, 512 normal, 768 detail-oriented requests"
    local_inference_timeout: "runInference and structured-action inference allow up to six minutes by default (HEMLOCK_INFERENCE_TIMEOUT_MS overrides); cancellation and steering remain immediate"
  agent_artistic_coding_smoke: "Live Electron proof: casual opener completed as conversation without a plan; approved five-step animation plan completed repo-map, artifact.create, artifact.author, preview.open, and preview.inspect using an explicitly recorded host fallback when Maple structured output was unavailable; artifact revision is previewable with digest and evidence; the pre-terminal live process blocked on Maple's malformed final envelope before the terminal fallback patch was reloaded, while the evidence-backed terminal fallback is covered by a passing unit test"
  agent_read_tools: "repo.inspect, file.read, file.search, git.status, git.diff, test.discover, verification.list, receipt.inspect"
  agent_plan_gate: "intent acceptance creates a durable waiting_for_approval plan; no action runs before explicit approval"
  agent_observation_projection: "actions and observations persist in the workspace projection and append-only projection journal"
  context_source_registry: "common adapter health/query/redaction contract; personal lanes remain opt-in and provenance-bearing"
  tool_use_evaluation: "11 fixtures; reports completion, valid actions, evidence coverage, false success, and forbidden commands"
  storage_guard: "Dream blocks below 10 GiB free and emits dream.blocked"
  electron_model_resolution: "HEMLOCK_MODEL_PATH/MAPLE_MODEL_PATH, then ~/Models/Hemlock/maple-2bit-mlx, then repo fallback"
  full_repo_pytest: "not green in current test environment: .venv has no pytest; system pytest lacks mlx/transformers and collected 9 import errors before interruption"
  note: Electron accepts MAPLE_DEV_URL for development when port 5173 is occupied; packaged mode loads the built dist/index.html.

live_workspace_v3:
  status: implemented_with_previewability_audit_passed
  tranche: "Hemlock OS Live Workspace"
  implementation_truth:
    window_manager:
      schema: hemlock.window.state.v2
      module: dream-chat/src/windowManager.js
      behavior: "deterministic bounds, all-edge/corner resize, canvas clamp, 16px snap, Option bypass, cascade open, compact z-order, exact restore, keyboard placement, v2 migration, narrow CSS stack"
      tests: "dream-chat/src/windowManager.test.js"
    renderer_boundaries:
      stream_store: dream-chat/src/streamStore.js
      window_frame: dream-chat/src/components/WindowFrame.jsx
      artifact_studio: dream-chat/src/main.jsx
      fonts: "@fontsource/dm-sans, @fontsource/dm-mono, @fontsource/newsreader bundled through Vite"
    chat_readability:
      behavior: "darker moss hierarchy for task metadata, labeled Maple channels, host telemetry, timestamps, trace summaries, status, and composer placeholder"
      contrast_detail: "supporting text is no longer dependent on pale gray alone; readable size and structural grouping remain paired with color"
    electron_authority:
      stream_protocol: dream-chat/electron/stream_protocol.cjs
      artifact_registry: dream-chat/electron/artifact_registry.cjs
      preview_policy: dream-chat/electron/preview_policy.cjs
      renderer_sandbox: true
      context_isolation: true
      node_integration: false
      public_interfaces: "agent.artifacts(action,input), agent.preview(action,input), agent.subscribeStream(callback)"
    stream_contract:
      schema: hemlock.agent.stream.v1
      kinds: [model_text, artifact_source, stdout, stderr, preview_console, training_log]
      behavior: "ordered sequence frames, UTF-8/SSE boundary parser, buffered fallback label, cancellation/steering abort, durable checkpoints and terminal digests"
    artifact_contract:
      schema: hemlock.agent.artifact.v1
      storage: "~/Library/Application Support/Hemlock/workspaces/<workspaceId>/tasks/<taskId>/artifacts/<artifactId>/"
      statuses: [drafting, previewable, ready, failed, superseded, exported_to_change_set]
      commands: [artifact.create, artifact.author, artifact.update, artifact.inspect, artifact.compare, artifact.freeze, artifact.export, artifact.preview.open, artifact.preview.inspect, artifact.preview.interact, artifact.preview.stop]
    artifact_panel_layout:
      behavior: "scrollable complete workspace with persistent local layout"
      resizers: "Source/Diff, Diff/Preview, and Output/Inspection dividers; pointer drag plus arrow-key control"
      storage_key: hemlock-artifact-layout-v1
      narrow_behavior: "single-pane tabs remain scrollable; desktop dividers are hidden when tabs are active"
  preview_contract:
    sandbox: "allow-scripts allow-forms iframe; no same-origin, popups, downloads, network, host APIs, file URLs, or arbitrary eval"
    budgets: "24 actions, 2 retries/action, 8 screenshots, 300000ms"
    harness: "typed DOM/accessibility/console/resize/click/type/key/scroll/hover/focus/wait messages"
    live_artistic_task:
      task_id: "task-2026-08-14T23-47-54-633Z-d9bd7"
      artifact_id: artifact-1786751403630
      revision: 1
      digest: "sha256:386531ba96892b3787c15658154888908aee20fcb970a24c125c21488a5d9bc5"
      status: previewable
      evidence: "authoring.host_fallback, artifact.preview.ready, artifact.inspection.completed"
      screenshot_a: "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-2026-08-14T23-47-54-633Z-d9bd7/receipts/eastern-hemlock-night-garden-stable-a.jpg"
      screenshot_b: "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-2026-08-14T23-47-54-633Z-d9bd7/receipts/eastern-hemlock-night-garden-stable-b.jpg"
  conversational_routing:
    casual_opener: "Hey Maple, how are you holding up? I want to make something beautiful together."
    result: "conversation complete; no plan proposed; 7.2s SSE response with durable digest"
    follow_up: "That garden has a lovely hush. I am back for a moment—what detail do you like best about it?"
    result_follow_up: "conversation complete; 17s SSE response with durable digest"
    mixed_revision: "I love the hush so far. One small revision: could you make the animation feel a little more alive without turning it busy—soften the mist, let two fireflies arrive later, and give the title a bit more breathing room on narrow screens? Keep the slow nocturnal mood."
    result_revision: "five-step plan proposed and approved; repo-map completed; subsequent local structured-action inference was stopped after extended model latency before revision completion; no false completion claimed"
  validation:
    node_syntax: "passed in final focused run before handoff"
    agent_contract_tests: "36 passed in latest run"
    ui_unit_tests: "9 passed in latest run"
    vite_build: "passed in latest run"
    electron_smoke: "passed fresh Electron host; artifact.create/author/preview.open/inspect/compare/export receipts observed"
    electron_launch_reload: "paired npm run dev + npm run electron loaded the production renderer path for 5s with no renderer console error; stopped intentionally after launch smoke, no interaction replay in this final pass"
    responsive_visual: "preview-first layout visually checked in Electron and browser at wide and narrow sizes; full-panel scroll and resizer pass checked in fresh Electron"
    chat_readability_visual: "paper Chat/Code surface checked after contrast and metadata sizing pass"
    renderer_dynamic_text_guard: "displayText now normalizes event, receipt, context, candidate, map, memory, stream, and status payloads before React renders them; focused tests and production build passed; paired Electron launch loaded the renderer after the fidelity patch with no console error, but no action-driven UI replay was run in the final pass"
    artistic_artifact_proof: "prior real Electron starter artifact authored, previewed, inspected, compared, and exported to an approval-gated change set; deterministic full lifecycle fixture now passed with revision repair, desktop/narrow snapshots, cancellation, and last-known-good proof"
    deterministic_artistic_fixture:
      command: "cd dream-chat && npm run test:e2e:artistic-fixture"
      status: passed
      receipt: "~/Library/Application Support/Hemlock/e2e/fixture/run-1786757062581/receipts/artistic-fixture-1786757062612.json"
      desktop_snapshot: "~/Library/Application Support/Hemlock/e2e/fixture/run-1786757062581/screenshots/desktop.svg"
      narrow_snapshot: "~/Library/Application Support/Hemlock/e2e/fixture/run-1786757062581/screenshots/narrow.svg"
      proof: "three Maple channels, approved plan, artifact r1 -> repaired r2 -> failed r3, parent/new digests, preview interactions, hidden screenshot block, paused input, prose-surrounded action envelope, and late callback suppression"
    real_maple_artistic_lane:
      command: "cd dream-chat && npm run test:e2e:artistic-maple"
      status: blocked
      endpoint: "http://127.0.0.1:8080"
      receipt: "~/Library/Application Support/Hemlock/e2e/maple/artistic-maple-1786756175215.json"
      result: "endpoint was healthy; real Maple completed a streamed casual response with role/reasoning/content channels and a parsed structured artifact.create envelope after 364548ms; the runner still stops before host artifact execution and keeps the lane blocked rather than claiming completion"
    performance_gates: pending
    previewability_receipts:
      screenshots:
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-00-16-253Z-86158/receipts/electron-artifact-preview-first.jpg"
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-00-16-253Z-86158/receipts/browser-artifact-preview-first.jpg"
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-00-16-253Z-86158/receipts/browser-narrow-stacked.jpg"
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-25-22-180Z-96852/receipts/artifact-studio-scroll-top.jpg"
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-25-22-180Z-96852/receipts/artifact-studio-scroll-evidence.jpg"
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-25-22-180Z-96852/receipts/artifact-studio-keyboard-resize.jpg"
        - "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-25-22-180Z-96852/receipts/chat-readability.jpg"
      artifact_manifest: "~/Library/Application Support/Hemlock/workspaces/workspace-e10fcf1345df2de3/tasks/task-session-2026-08-14T22-00-16-253Z-86158/artifacts/artifact-1786744977948/manifest.json"
      change_set_manifest: "~/Library/Application Support/Hemlock/workspace-runtime/workspaces/changesets/artifact-artifact-1786744977948-r1/manifest.json"
  unmet_acceptance:
    - "The real Maple endpoint is now reachable and slow local inference is no longer cut off at 180 seconds, but the real lane remains blocked before host artifact execution because the current runner intentionally does not drive the Electron bridge."
    - "Full Maple-backed streaming/steering runtime proof still needs a live Electron replay with the extended timeout; the direct real lane now proves Maple channel preservation and parsed structured output."
    - "Target-M1 pointer/paint, delta-to-paint, artifact-refresh, long-task, and ten-minute buffer timing gates were not measured in this pass."
    - "A full ambitious multi-revision Maple-authored garden and bounded repair remain unproven in the real Maple lane; the deterministic fixture proves host behavior with an artistic fixture source."
media_redaction:
  status: complete
  source: "/Users/ianzvirbulis/Desktop/Screen Recording 2026-08-14 at 9.28.45 PM.mov"
  output: "/Users/ianzvirbulis/Desktop/Screen Recording 2026-08-14 at 9.28.45 PM-censored.mov"
  duration_seconds: 27.9
  video: "2870x1620 H.264 60fps"
  original_preserved: true
  redactions:
    receipts: "Opaque redaction over the Receipts content region during 18.8-21.3s, covering the path lines through the scroll transition."
    settings_runtime_storage: "Opaque redaction over the Runtime Storage path region during 23.25-24.7s."
  verification:
    ffprobe: passed
    encoded_ocr: "0 suspicious username matches across 140 frames sampled at 5fps with macOS Vision OCR."
    visual_checks: "Rendered frames checked at 18.8, 19.2, 19.8, 20.8, 21.3, 23.4, 24.4, and 24.7 seconds."

video_showcase:
  status: rendered_and_verified
  project: video-showcase
  sources:
    - "/Users/ianzvirbulis/Desktop/Screen Recording 2026-08-14 at 10.38.46 PM.mov"
    - "/Users/ianzvirbulis/Desktop/Screen Recording 2026-08-14 at 10.39.32 PM.mov"
  source_preserved: true
  output: video-showcase/exports/kin-timber-showcase-cut.mp4
  ledger_asset: ".media/video/video_001.mp4"
  duration_seconds: 41.033008
  format: "1920x1080 H.264 yuv420p BT.709 at 30fps"
  audio: "none; both supplied sources had no audio stream"
  edit: "Eight selected windows with 0.18-second fades; UI pixels preserved; original 1.97:1 capture letterboxed into 16:9 without cropping."
  proof:
    render: passed
    ffprobe: passed
    decode: passed
    visual_review: "normal, reduced/squint, blurred readability sheets, and transition-neighborhood checks inspected"

video_showcase_revision_2:
  status: rendered_and_verified
  project: video-showcase
  source_new: "/Users/ianzvirbulis/Desktop/Screen Recording 2026-08-14 at 10.53.45 PM.mov"
  source_existing: "video-showcase/exports/kin-timber-showcase-cut.mp4"
  source_preserved: true
  output: "video-showcase/exports/kin-timber-showcase-cut-v3-smoothed.mp4"
  ledger_asset: ".media/video/video_002.mp4"
  duration_seconds: 49.466667
  format: "1920x1080 H.264 yuv420p BT.709 at 30fps"
  audio: "none; new source had no audio stream"
  edit: "Construction arc added at the front; eight windows total with 0.18-second fades; UI pixels preserved; 16:9 letterbox without cropping."
  frame_rate_smoothing: "Scene-aware linear interpolation from the new source's uneven roughly 23.44fps average to constant 30fps; existing first-pass material remained 30fps."
  proof:
    render: passed
    ffprobe: passed
    decode: passed
    visual_review: "normal, reduced/squint, blurred readability sheets, and transition-neighborhood checks inspected"

video_showcase_revision_3_x_paced:
  status: rendered_and_verified
  project: video-showcase
  target: "X post"
  source_new: "/Users/ianzvirbulis/Desktop/Screen Recording 2026-08-14 at 10.53.45 PM.mov"
  source_existing: "video-showcase/exports/kin-timber-showcase-cut.mp4"
  source_preserved: true
  output: "video-showcase/exports/kin-timber-showcase-cut-x-paced.mp4"
  ledger_asset: ".media/video/video_003.mp4"
  duration_seconds: 30.2
  format: "1920x1080 H.264 yuv420p BT.709 at 30fps"
  audio: "none; source material had no audio stream"
  edit: "Seven shorter beats; immediate construction hook; six 0.14-second fades; UI pixels preserved; 16:9 letterbox without cropping."
  frame_rate_smoothing: "Scene-aware linear interpolation from the new source's uneven roughly 23.44fps average to constant 30fps; existing first-pass material remained 30fps."
  proof:
    render: passed
    ffprobe: passed
    decode: passed
    visual_review: "normal, reduced/squint, blurred readability, and all six transition neighborhoods inspected"

provider_integration:
  status: implemented_with_claude_reauth_required
  lanes:
    maple_preview:
      label: Maple-Preview
      runtime: local MLX OpenAI-compatible endpoint at http://127.0.0.1:8080
      reasoning: native
    codex:
      label: Codex
      runtime: "authenticated codex exec JSONL subprocess"
      sandbox: read-only
      reasoning: [low, medium, high, xhigh, max]
    claude:
      label: Claude
      runtime: "claude auth-backed stream-json subprocess"
      reasoning: [low, medium, high, xhigh, max]
  picker:
    surface: top-right system bar
    persistence: localStorage key hemlock-model-selection-v1
    controls: provider lane, provider model, reasoning level
  settings:
    surface: Subscription providers
    actions: [log_in, re_authenticate, log_out, refresh_status]
    login_flow: "opens the provider's own interactive CLI flow in Terminal"
    credential_boundary: "Hemlock never reads, asks for, or stores provider API keys, OAuth tokens, or subscription credentials"
  host_routing:
    conversation: "selected lane streams through the Electron host and preserves provider/source/raw-output receipts"
    structured_actions: "selected external lanes are bounded to exact JSON action output; unsupported or malformed output remains blocked"
  verification:
    provider_parser_tests: passed
    agent_tests: "41 passed"
    ui_tests: "10 passed"
    build: passed
    browser_preview: "top-right picker, Codex reasoning menu, and Settings provider rows visually inspected"
    codex_live_smoke: "passed; bounded prompt returned OK"
    claude_live_smoke: "blocked; CLI returned HTTP 401 authentication_failed because the local OAuth access token was revoked"
  claim_boundary: "The integration and manual login controls are implemented. Codex is live-verified through the authenticated CLI. Claude requires the user to choose Re-authenticate in Hemlock Settings, complete Claude's own login flow, then refresh status; local auth status alone is not treated as proof of a usable inference token."

stability_pass_2026_08_15:
  status: verified_with_codex_luna
  requested_lane: "GPT-5.6 Luna through the Codex subscription lane; Maple-Preview and Dream inference were intentionally not run."
  changes:
    - "External Codex/Claude conversation inference now restarts after an accepted steering update, matching the existing Maple steering contract."
    - "Provider CLI startup, nonzero exit, empty output, and authentication failures now leave a conversation task blocked with provider/model/reasoning, stream, and raw-output receipt context. Structured-action failures remain available to the existing repair/fallback loop."
    - "Activity work notes and structured-action start events identify the selected provider instead of hard-coding Maple."
  luna_delegation:
    preflight: passed
    role: goal_scout
    result: blocked_by_existing_goal_board
    note: "The named GPT-5.6 Luna scout returned goal-role-receipt.v1 but declined inspection because docs/goals/maple-dream-proper-app/state.yaml has one active Worker task T004; it did not reinterpret that task as a Scout audit."
    configured_model: gpt-5.6-luna
    runtime_model_slug: unverified
  verification:
    node_syntax: passed
    agent_tests: "41 passed"
    ui_tests: "10 passed"
    build: passed
    electron_desktop_startup: "passed; npm run desktop started Vite and Electron without a startup crash, then was stopped cleanly; no model inference was invoked"
    yaml_parse: passed
    diff_check: passed
    codex_live_smoke: "passed with model gpt-5.6-luna; bounded prompt returned OK"
    browser_preview: "passed; top-right Codex picker, gpt-5.6-luna model, max reasoning, Claude model/reasoning controls, Settings provider rows, reload persistence, and no app console errors"
  unchanged_boundaries:
    - "Maple-Preview remains available in the picker but was not invoked during this pass."
    - "Claude live inference remains blocked by the previously observed revoked local OAuth token until the user completes Re-authenticate in Hemlock Settings."
    - "Browser preview cannot prove desktop CLI login or Electron subprocess execution; the Codex live smoke was run directly through the authenticated Codex CLI."

launch_shortcut_2026_08_15:
  status: implemented
  launcher: "Launch Hemlock.command at the repository root"
  terminal_alias: "cd dream-chat && npm run launch"
  model_picker_shortcut: "Cmd-Shift-M toggles the top-right model picker"
  verification:
    launcher_syntax: passed
    launcher_smoke: "passed; quoted ./Launch Hemlock.command started npm run launch, Vite, and Electron, then was stopped cleanly"
    picker_shortcut: "passed; Cmd-Shift-M opened and then closed the model picker in the rendered preview"
    verify_agent: "passed; 41 agent tests, 10 UI tests, and production build"
  claim_boundary: "The launcher starts the normal Vite plus Electron development pair; it does not start Maple, Dream, or a provider inference request by itself."

performance_stability_2026_08_15:
  status: implemented_with_measured_baseline
  runtime_launch:
    command: "maple.launch"
    surface: "Dream Lab / Maple-Dream runtime card"
    behavior: "Starts the local Maple process when needed, waits for /health, and deduplicates concurrent launch requests."
    stability: "A child-process exit before readiness fails fast instead of waiting for the full readiness timeout."
    inference_boundary: "Process health is reported separately from inference verification; the launch action sends no chat completion request and does not start Dream training."
    receipt: "hemlock.maple.launch.v1"
    failure_handling: "Maple child-process error and pre-readiness exit details are preserved in the launch failure receipt instead of waiting for a generic readiness timeout."
  streaming:
    status: implemented
    change: "Maple stream deltas are accumulated per channel for one 16 ms host-to-renderer dispatch window; the terminal frame flushes queued content synchronously."
    compatibility: "The host still accumulates exact channel text and emits monotonically increasing stream sequences; only redundant IPC frame frequency is reduced."
    synthetic_probe: ".optimize/maple_stream_dispatch_probe.cjs"
    synthetic_result: "4,000 source deltas became 2 IPC frames, a 99.95% reduction for the measured two-channel burst. This is an IPC result, not a claim about Maple token throughput."
    unit_tests: "3 passed"
  renderer:
    closed_surfaces: "Closed windows no longer construct their full React surface; minimized windows retain their title bar without constructing hidden body content."
    provider_status: "Codex and Claude CLI status checks are deferred until the model picker is opened or Settings Refresh is used; concurrent refreshes share one in-flight request."
  verification:
    maple_runtime_receipt_and_bridge_tests: "5 passed"
    agent_tests: "47 passed"
    ui_tests: "10 passed"
    build: passed
    node_syntax: passed
    yaml_parse: passed
    diff_check: passed
    browser_preview: "passed; Dream Lab shows a disabled desktop-only Launch Maple / Dream button in browser preview with an explicit no-inference/no-training note"
    electron_desktop_startup: "passed; npm run desktop started Vite and Electron without a startup crash, then was stopped cleanly; no Maple process or inference was invoked"
  measured_devloop_baseline:
    probe_file: ".optimize/probes.sh"
    before_run: ".optimize/runs/20260815T154539.json"
    after_run: ".optimize/runs/20260815T155327.json"
    medians_seconds:
      build: "0.462 -> 0.403"
      agent_tests: "0.389 -> 0.384"
      ui_tests: "0.284 -> 0.243"
      syntax: "0.111 -> 0.092"
    interpretation: "All probes stayed green. No wall-clock result exceeded the optimization skill's >5% or >2s acceptance threshold; the renderer/runtime changes are recorded as structural performance and stability work, not as a claimed timed speedup."
  measured_maple_runtime:
    health_probe: ".optimize/maple_health_probe.sh"
    before_run: ".optimize/runs/20260815T-maple-baseline.json"
    after_run: ".optimize/runs/20260815T-maple-after.json"
    full_regression_run: ".optimize/runs/20260815T-maple-full-after.json"
    health_ready_seconds: "5.228 -> 3.264; the faster after sample is recorded but not attributed to the code change because model-load timing is cache/system-state sensitive and the probe runs once per sample."
    full_regression_medians_seconds: "build 0.366, agent-tests 0.577, ui-tests 0.301, syntax 0.118, maple-health 4.040, maple-stream-dispatch 0.047; all probes green."
    stream_dispatch_seconds: "0.034 median over the focused after run and 0.047 median in the full regression run; the probe remained green after the change."
  claim_boundary: "Maple/Dream launch is wired and host-boot verified. A successful local model health check and a browser button do not prove Maple inference quality, Dream training success, or provider authentication."

maple_preview_agent_reliability_2026_08_15:
  status: implemented_verified
  interaction_mode:
    payload_field: interactionMode
    values: [explore, build]
    default: explore
    build_handoffs: ["build this", "make the artifact", "create the draft", "open a working version", "implement this", "fix this bug"]
    behavior: "Visual ideation stays conversational; Build or a clear handoff creates the coding plan and preserves the single plan approval gate."
  host_action_compiler:
    command_identity: "Host always supplies the current planned commandId, kind, approval, evidence, taskId, step, and lifecycle status."
    malformed_evidence: "One-key evidence objects normalize to string arrays; invalid values fall back to host plan evidence."
    terminal_action: "Host creates the deterministic terminal action after the final planned command without another Maple inference."
  artifact_autopilot:
    storage: "Task-scoped Hemlock Application Support scratch artifacts; repository source remains untouched."
    receipts: [manifest, revision, preview_session, inspection_report, verification_receipt, repair_receipt]
    preview_handshake: "Renderer reports readiness, DOM/accessibility summaries, and console errors through the dedicated preview-report bridge; host validates task, artifact, revision, session, static source, and rendered content."
    repair: "At most two bounded full-source or complete-file patch repairs; invalid candidates are rolled back and never become the last-good revision."
    exhausted_actions: ["Retry repair", "Use last good revision"]
  deterministic_fixture:
    command: "npm run test:e2e:artifact-autopilot"
    scenario: "Malformed action, commandId none, scratch creation, preview runtime error, malformed repair, successful repair, receipt-backed completion."
    result: "completed=true; inferenceCalls=7; terminalInferenceCalls=0; repairCalls=2; repairAttempts=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
  verification:
    agent_tests: "65 passed"
    ui_tests: "10 passed"
    build: passed
    node_syntax: passed
    yaml_parse: passed
    diff_check: passed
    electron_desktop_startup: "passed; npm run desktop reached Vite ready and Electron started without a startup crash, then was stopped cleanly; no model inference was invoked"
    final_measurement: ".optimize/runs/20260815T-final-verify-2.json"
  claim_boundary: "The deterministic host contract and repair behavior are proven locally. Live Maple artifact inference remains optional and is not claimed by the fixture; no repository mutation is automatic."

maple_preview_hemlock_native_agent_2026_08_15:
  status: implemented_verified
  durable_threads:
    implementation: "ThreadManager persists independent projects, user-assigned workspace roots, thread metadata, checkpoints, suggestions, provider caps, writer leases, and per-thread conversation JSONL references under Hemlock Application Support."
    ui: "Chat exposes a thread/project switcher, New thread flow, current workspace scope, provider/model context, status, and host-generated suggestion cards. Switching restores the selected thread's task projection and conversation history reference."
    restart_boundary: "Unfinished work is restored as a resumable blocked/paused projection; startup does not resume execution without an explicit user action. Workspace digest is revalidated on resume."
  provider_scheduler:
    defaults: { maple: 1, codex: 2, claude: 2 }
    behavior: "Maple-Preview is serialized by a host-owned provider lease; Codex and Claude use bounded independent lanes. Provider changes are explicit and affect the next request/resume, never an inference already in flight."
    controls: "Provider caps are persisted through provider.capacity and exposed in the Command Center scheduler panel."
  coding_autonomy:
    plan: [context.refresh, repo-map, repo.inspect, git.status, code.apply, verify, git.diff]
    mutation: "Complete source maps or bounded complete-file patches are restricted to the assigned workspace, guarded by a same-directory writer lease, secret-sensitive path rejection, base-digest checks, before/after digests, rollback manifests, and change-set receipts."
    repair: "Coding verification failures get at most two host-owned repair passes; failed candidates roll back and the last-good change-set remains active."
    completion_gate: "A general coding task cannot complete without an applied hemlock.agent.change-set.v1 and a passing hemlock.agent.verification.v1 receipt. Artifact tasks retain the preview verification gate."
    escalation: "Maple failures produce an unread provider-escalation suggestion for explicit Codex/Claude retry; there is no silent provider fallback."
  context_and_metrics:
    checkpoint_fields: "Thread/task/provider/model/reasoning/phase/active plan step/pending action/evidence/workspace digest/repair/issues/autonomy are durable at lifecycle boundaries."
    conversation: "User and assistant conversation entries are stored per thread with provider/model/reasoning and raw output references; unrelated thread history is not sent as inference context."
  verification:
    agent_tests: "65 passed"
    ui_tests: "10 passed"
    build: passed
    syntax: passed
    agent_evaluation: "validActionRate=1; falseSuccessCount=0 on the supplied trace evaluator"
    artifact_fixture: "completed=true; inferenceCalls=7; terminalInferenceCalls=0; repairCalls=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
    desktop_startup: "npm run desktop reached Vite ready and started Electron; the bounded smoke was stopped cleanly after startup. No Maple, Dream, Codex, or Claude inference was invoked."
  claim_boundary: "The host-owned thread/scope/scheduler/coding contracts and deterministic artifact repair fixture are verified locally. Live multi-provider concurrency, live Maple coding quality, and full simultaneous kernel projection rehydration remain runtime-dependent and were not claimed by these tests."

live_maple_validation_2026_08_15:
  status: blocked_on_model_action_format
  command: "HEMLOCK_MAPLE_MAX_MS=120000 npm run test:e2e:artistic-maple"
  health: "passed; /health returned status ok after MAPLE_AUTOSTART_SERVER=1 npm run desktop"
  conversation: "passed; real Maple streaming returned non-empty content with reasoning and content channels"
  structured_action: "blocked; parseStatus invalid JSON, finish_reason length, completion token budget 4096; the response included an incomplete/non-contract artifact action envelope"
  elapsed_ms: 80752
  receipt: "/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-maple-1786835168879.json"
  claim_boundary: "The real Maple process and conversation path were exercised. The endpoint did not complete a structured artifact action, so no host artifact mutation or completion was claimed."

maple_action_contract_and_variation_fix_2026_08_15:
  status: implemented_verified
  host_contract:
    compact_context: "Structured-action inference now sends a compact task/context/next-step payload without full conversation history or duplicate raw task state."
    budgets: "Non-authoring Maple action turns use max_tokens 1536; artifact.author, artifact.update, and code.apply turns retain 8192 source headroom."
    payload_shapes: [full_action_envelope, compact_bare_payload, direct_relative_file_map]
    recovery: "Host can recover a truncated envelope's registered command metadata, coerce a bare payload into the current planned command, filter command-specific inputs, and still validate allowlist, scope, approval, and evidence host-side."
    artifact_create: "Only metadata is accepted; source/html/data/patch payloads are discarded until the later author step. Invalid artifact kind/entrypoint values receive host defaults."
    authoring_variation: "Valid source is preserved as model-owned input; the host fallback is used only for malformed/incomplete authoring and is not the normal visual template."
  deterministic_variation_fixture:
    command: "npm run test:e2e:artistic-variations-fixture"
    result: "requested=4; passed=4; uniqueSourceCount=4; diversity=varied"
    styles: [css_dom, svg_botanical, canvas_particles, kinetic_cards]
  verification:
    agent_tests: "70 passed"
    ui_tests: "10 passed"
    build: passed
    syntax: passed
    artifact_autopilot: "completed=true; inferenceCalls=7; terminalInferenceCalls=0; repairCalls=2; repairAttempts=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
  live_compact_action:
    command: "HEMLOCK_MAPLE_MAX_MS=120000 npm run test:e2e:artistic-maple"
    status: host_normalizable
    conversation: "passed; real Maple streaming returned role/reasoning/content channels"
    structured_action: "returned a compact bare metadata object rather than the requested full envelope; host coercion produced a hemlock.agent.action.v1 artifact.create action with taskId, commandId, approval, and evidence"
    source_execution: "not claimed; the standalone runner intentionally does not mutate or preview an artifact"
    elapsed_ms: 26044
    receipt: "/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-maple-1786835972852.json"
  live_variation_matrix:
    command: "HEMLOCK_MAPLE_MAX_MS=180000 npm run test:e2e:artistic-variations"
    status: blocked_on_local_authoring_budget
    result: "requested=4; passed=0; failed=4; the first long source-authoring turn returned no usable content before the shared 180000ms budget expired, leaving insufficient budget for the remaining variants"
    receipt: "/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-variations-1786836159847.json"
    thinking_disabled_probe: "HEMLOCK_MAPLE_MAX_MS=90000 HEMLOCK_MAPLE_VARIATION_LIMIT=1 npm run test:e2e:artistic-variations also returned no complete input.source before its 90000ms budget; chat_template_kwargs.enable_thinking=false is wired but did not establish live authoring success on this run"
    thinking_disabled_receipt: "/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-variations-1786836576864.json"
    claim_boundary: "Live Maple source-variation quality and renderer correctness remain unproven. The host contract and deterministic four-style variation fixture are green; the local model needs a shorter/faster authoring strategy or a larger bounded authoring budget for live multi-variant proof."

maple_reasoning_streaming_variation_followup_2026_08_15:
  status: implemented_with_live_model_boundaries
  changes:
    reasoning: "Removed the structured-action thinking suppression. Maple requests now explicitly use chat_template_kwargs.enable_thinking=true; reasoning is recorded and streamed rather than discarded."
    live_streaming: "Structured Maple actions use SSE through an internal agent_action stream; the renderer keeps that internal trace in Activity while only model_text streams materialize as conversational messages."
    server_output_ceiling: "Raised the local Maple server ceiling from 512 to 4096 output tokens so reasoning does not consume the entire authoring response before source is emitted. Per-request budgets remain host-controlled."
    payload_normalization: "Host coercion now accepts Maple's flattened input.source authoring shape as well as nested input.source and direct relative-file maps."
    variation_probe: "Live variation probes use streaming plus reasoning, support selecting individual styles, cap index.html to 2200 characters, and record bounded channel diagnostics on failure."
  verification:
    agent_tests: "71 passed"
    ui_tests: "10 passed"
    build: passed
    artifact_autopilot: "completed=true; inferenceCalls=7; terminalInferenceCalls=0; repairCalls=2; repairAttempts=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
    variation_fixture: "requested=4; passed=4; uniqueSourceCount=4; diversity=varied"
    syntax: "all electron/*.cjs syntax valid"
    state_yaml: valid
    diff_check: passed
    reasoning_disable_scan: "no enable_thinking=false paths remain under dream-chat"
    live_action: "conversation and structured artifact.create action both streamed role/reasoning/content channels; host normalized the action; receipt=/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-maple-1786837099710.json"
    live_css_dom: "passed; one real Maple CSS/DOM animation produced a bounded 2250-character source map with css-animation signature; receipt=/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-variations-1786837455164.json"
    live_full_matrix: "requested=4; passed=1; failed=3 by the shared 240000ms local Maple budget; CSS/DOM passed, later SVG/canvas/kinetic turns did not complete usable content; receipt=/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-variations-1786837708924.json"
    live_svg_isolated: "requested=1; passed=0; Maple emitted SVG source in the reasoning channel but finished with length before a complete content action; host correctly did not claim it; receipt=/Users/ianzvirbulis/Library/Application Support/Hemlock/e2e/maple/artistic-variations-1786837801717.json"
  claim_boundary: "Reasoning and live streaming are now enabled in the runtime contract, and one real Maple animation variation passed host source-shape validation. The remaining live styles still need a compact authoring/retry strategy and renderer verification; deterministic four-style diversity remains green and is not substituted for live-model proof."

maple_reasoning_no_forced_budget_followup_2026_08_15:
  status: implemented_verified
  policy: "Maple reasoning is always enabled for local conversation, structured actions, and artistic variation probes. The prompt no longer asks Maple to keep reasoning short, and the old 1536-token ordinary-action versus 8192-token authoring split is removed."
  runtime: "The host uses a shared configurable Maple output ceiling: 16384 tokens by default, with HEMLOCK_MAPLE_MAX_TOKENS available for explicit tuning and a host floor of 4096. This is transport/server capacity, not a reasoning instruction; Maple may stop naturally when it is done. Invalid environment values fall back safely to the default."
  streaming: "Conversation uses the host SSE path and structured actions use an internal agent_action SSE path. Reasoning and content deltas are retained as separate channels; only conversational model_text streams become Chat messages, while action reasoning remains visible in Activity."
  boundaries: "Source-file size, path scope, safety approval, artifact repair count, preview verification, and inference wall-clock cancellation remain host-owned reliability boundaries. They do not impose a short reasoning-word budget."
  verification:
    agent_tests: "71 passed"
    ui_tests: "10 passed"
    build: passed
    artifact_autopilot: "completed=true; inferenceCalls=7; terminalInferenceCalls=0; repairCalls=2; repairAttempts=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
    variation_fixture: "requested=4; passed=4; uniqueSourceCount=4; diversity=varied"
    syntax: "main.cjs, e2e_artistic_maple.cjs, and e2e_artistic_variations.cjs valid"
    state_yaml: valid
    diff_check: passed
  live_boundary: "The prior live matrix remains the honest model-quality result: one CSS/DOM variation passed, while SVG/canvas/kinetic attempts did not complete usable source within the shared local budget. The host did not claim those failures as completed artifacts; deterministic four-style variation coverage remains green."

maple_adaptive_capability_loop_2026_08_15:
  status: implemented_verified
  model_freedom: "An approved plan is now a user-approved capability boundary and starting direction, not a positional command script. Maple receives the current evidence, the current planned step, and an allowlisted allowedNextCommands set, then may select the next useful safe read, inspect, verify, artifact, preview, or plan-approved write action."
  adaptive_plan: "When Maple selects an allowlisted action outside the current step, Hemlock either moves that action forward from the remaining approved plan or inserts a host-owned adaptive step. The plan records adaptiveDecisions, plan.adapted is emitted, and action.hostSelection records requested versus selected command identity."
  recovery: "Missing or none commandIds fall back to the current approved step. Unknown, unsafe, or unavailable model commands fall back to that step with action.command.recovered evidence instead of unnecessarily killing the whole task. Inputs are filtered through the command-specific host contract before execution."
  model_control: "Maple can return ask_user, blocked, or answer when that is the genuinely useful next state; the host preserves those terminal/pausing intentions while retaining lifecycle and completion gates."
  safety_boundary: "Adaptive selection is limited to registered safe capabilities. Training, runtime, external/network, secret-sensitive, destructive, and explicit-only operations cannot be introduced by model choice. Workspace scope, approval, verification, and receipt-backed completion remain host-owned."
  verification:
    agent_tests: "74 passed"
    ui_tests: "10 passed"
    build: passed
    artifact_autopilot: "completed=true; inferenceCalls=7; terminalInferenceCalls=0; repairCalls=2; repairAttempts=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
    variation_fixture: "requested=4; passed=4; uniqueSourceCount=4; diversity=varied"
    syntax: "agent_kernel.cjs, agent_contracts.cjs, and agent_orchestrator.cjs valid"
    state_yaml: valid
    diff_check: passed
  claim_boundary: "The deterministic loop now proves adaptive safe action selection, unavailable-command recovery, and user-question pausing. Live Maple still remains non-deterministic; these host changes do not claim that every local model variation will author or verify successfully."

hemlock_double_click_launcher_2026_08_15:
  status: implemented_verified_with_native_maple_smoke
  user_path: "Double-click Hemlock.app at the repository root; Launch Hemlock.command remains the terminal-friendly fallback."
  launcher: "scripts/launch-hemlock.zsh starts npm run desktop with MAPLE_AUTOSTART_SERVER=1, writes ~/Library/Logs/Hemlock/launch.log, and uses a PID-backed user-level lock to ignore duplicate starts and recover stale locks."
  app_bundle: "Hemlock.app/Contents/Info.plist and Hemlock.app/Contents/MacOS/Hemlock are executable and resolve the neighboring dream-chat worktree. A Finder alias is supported; moving the bundle away from the worktree is intentionally not treated as portable installation."
  runtime_fix: "Electron now pins universal Python child processes through /usr/bin/arch to the host architecture by default, with HEMLOCK_PYTHON_ARCH=arm64|x86_64 available for an intentional override. This prevents an x86_64 Python slice from loading the arm64-only MLX extension."
  verification:
    plist: "plutil -lint Hemlock.app/Contents/Info.plist passed"
    syntax: "node --check dream-chat/electron/main.cjs and zsh -n launcher scripts passed"
    dry_run: "Hemlock.app and Launch Hemlock.command both resolved npm and MAPLE_AUTOSTART_SERVER=1"
    actual_double_click: "open Hemlock.app started the launcher, Vite at http://127.0.0.1:5173/, Electron, and Maple with architecture=arm64"
    maple_health: "GET http://127.0.0.1:8080/health returned {status: ok}"
    duplicate_launch: "A concurrent Launch Hemlock.command returned 0 and logged the existing launcher PID without starting a second desktop"
  claim_boundary: "The app startup and Maple process-health path are verified. The smoke did not claim a completed model inference; Hemlock's UI still distinguishes process readiness from inference readiness."

maple_live_loop_and_preview_stability_2026_08_15:
  status: implemented_verified_after_runtime_restart
  handoff_detection:
    behavior: "Natural requests such as Build a new unique interactive animation artifact and Build an actual webapp enter Build mode; visual ideation and create-a-concept phrasing remain Explore unless there is a clear handoff. Renderer and host classifiers stay aligned."
  host_preview_progression:
    behavior: "artifact.preview.open and artifact.preview.inspect are host-deterministic approved-plan steps. Hemlock fills artifact/session identifiers from receipts and does not spend another Maple structured-action turn restating them. Repository inspection and safe adaptive selection remain model-driven."
    measured_fixture: "Artifact autopilot completed with inferenceCalls=5, terminalInferenceCalls=0, repairCalls=2, previewInspectionCalls=2, artifactRevisionCount=2, repositoryTouched=false, falseSuccess=false."
  maple_stream_contract:
    behavior: "Repeated role metadata is excluded from model output channels; reasoning and content remain streamed and durable. Structured-action parsing can recover an envelope emitted in a reasoning channel. Stored SSE payloads are compacted to deltas while channel text remains complete."
    observed_receipt: "The prior 5,112,972-byte streamed payload trace projects to 4,293,744 bytes under the compact payload representation, a 16.02% reduction; the source receipt remains untouched."
  ipc_stability:
    behavior: "agent:state and agent:cancel now receive bounded plain-data stream snapshots rather than active coalescers/controllers, preventing Electron structured-clone failures and preserving cancellation visibility."
    runtime_observation: "Before restart, launch.log recorded An object could not be cloned for agent:state and agent:cancel. After restart with the fix loaded, Maple health returned status ok and no new clone errors appeared in the observed launch log."
  verification:
    agent_tests: "81 passed"
    ui_tests: "10 passed"
    build: passed
    artifact_autopilot: "completed=true; inferenceCalls=5; terminalInferenceCalls=0; repairCalls=2; repairAttempts=2; previewInspectionCalls=2; artifactRevisionCount=2; repositoryTouched=false; falseSuccess=false"
    variation_fixture: "requested=4; passed=4; uniqueSourceCount=4; diversity=varied"
    syntax: "main.cjs, stream_protocol.cjs, agent_orchestrator.cjs, interaction_modes.cjs, and launch-hemlock.zsh passed syntax checks"
    diff_check: passed
    maple_health_after_restart: "GET http://127.0.0.1:8080/health returned {status: ok}; running desktop processes are the restarted launcher, Vite, Electron, and arm64 Maple server."
  claim_boundary: "The host no longer loops preview actions or fails state/cancel IPC on non-cloneable stream internals. The local Maple model remains non-deterministic: a long reasoning response may still take time, and no live artifact-quality claim is made by these host and fixture checks."

github_publish_2026_08_15:
  status: blocked_by_repository_permission
  target: "https://github.com/deepgrove-ai/mlx-lm-deepgrove"
  branch: agent/hemlock-maple-stability
  local_commit_before_receipt: "259d64c Add Hemlock-native Maple agent workflow"
  push_attempt: "git push -u origin agent/hemlock-maple-stability returned HTTP 403; no remote branch or PR was created."
  authenticated_account: RasputinKaiser
  repository_permission: "GitHub API reports viewerPermission=READ and permissions.push=false."
  excluded_dirty_roots:
    - .media/
    - video-showcase/
    - work/
  claim_boundary: "The scoped Hemlock changes are committed locally and ready for an account with write access. Merge and deployment cannot be claimed until the branch is pushed; this repository has no Hemlock deployment workflow configured."

github_repo_publish_2026_08_15:
  status: published
  repository: "https://github.com/RasputinKaiser/Hemlock-Local-AI"
  visibility: private
  default_branch: main
  published_commit: "8a936194912fd4eacbe83cc3ca4839ecc650b254"
  follow_up_receipt_commit: "f3194fb Record Hemlock-Local-AI publish receipt"
  verification: "GitHub API reports the repository exists, is private, defaults to main, and main contains Add Hemlock-native Maple agent workflow."
  excluded_dirty_roots:
    - .media/
    - video-showcase/
    - work/
  claim_boundary: "The dedicated GitHub repository and main branch are published. No deployment target is configured for this repository yet."

chat_plan_scroll_and_path_privacy_2026_08_16:
  status: implemented_verified
  plan_surface: "Chat now renders the durable plan and approval controls outside the conversation scroll. The plan steps use a bounded internal scroller, while Approve plan, Reject, and Refresh task state remain reachable."
  model_visibility: "The Chat surface distinguishes host-prepared planning from Maple execution, states explicitly when Maple has not run, and exposes live model_text channels in a dedicated live stream panel after approval."
  evidence_truth: "Pending plan approval forces the Chat evidence state to waiting and excludes stale observation/evidence references from the approval surface. Observations are scoped to the active task."
  path_privacy: "Displayed user-home paths are redacted to ~/... in UI text, queue labels, thread controls, and prompts. Host payloads and workspace scope enforcement continue using the real filesystem path."
  verification:
    ui_tests: "13 passed"
    build: passed
    maple_health: "GET http://127.0.0.1:8080/health returned {status: ok} after the live Vite/Electron session reloaded the UI."
    diff_check: passed
  claim_boundary: "This fixes the Chat/build presentation and display privacy boundary. It does not claim that Maple has run until a live inference stream or host receipt is present."

chat_transcript_and_collapsible_plan_dock_2026_08_16:
  status: implemented_verified
  layout: "Chat now keeps the conversation, Maple model/reasoning output, and host activity in the upper scrollable transcript. The approved or awaiting plan is docked below that transcript and immediately above Work mode and the composer."
  plan_controls: "The plan remains full-sized within a bounded dock, can be collapsed to its heading, and keeps Approve plan, Reject, or Refresh task state available when those controls are required."
  hero: "The task/objective hero is bounded to a compact two-line summary with a wider readable measure, leaving more vertical space for the transcript while retaining the full objective disclosure."
  stream_behavior: "The transcript follows new messages and streamed model channels only while the reader is already near the bottom. Scrolling upward to inspect prior user text, Maple prose/reasoning, actions, or evidence is preserved across subsequent stream updates."
  verification:
    ui_tests: "14 passed"
    agent_tests: "81 passed"
    build: passed
    syntax: "electron/main.cjs passed syntax check"
    diff_check: passed
  claim_boundary: "This verifies the renderer layout and scroll ownership contract. It does not claim a live artifact result; artifact completion still requires host receipts and verification."

chat_conversation_visibility_2026_08_16:
  status: implemented_verified
  conversation_restore: "On desktop state hydration, Hemlock switches to the active durable thread and restores its persisted user/model conversation into Chat without overwriting messages already present in the window."
  host_detail_priority: "Exact validated action envelopes and raw provider output references are collapsed by default in Chat so the readable user/model transcript remains primary; host evidence stays available on demand."
  verification:
    ui_tests: "16 passed"
    agent_tests: "81 passed"
    build: passed
    syntax: "electron/main.cjs passed syntax check"
    diff_check: passed
    maple_health: "GET http://127.0.0.1:8080/health returned {status: ok}"
  claim_boundary: "This improves Chat visibility and restores durable conversation rendering. It does not alter host action identity or artifact completion gates."

chat_conversation_first_layout_2026_08_16:
  status: implemented_verified
  hierarchy: "The transcript is now the only auto-following scroll surface. Host action/evidence detail is rendered in a separate compact rail beneath the transcript, followed by the collapsed plan control, work mode, and composer."
  progressive_disclosure: "Host activity is a collapsed summary by default and expands only on request. The plan remains a compact collapsed status/control surface after approval and expands for steps, rationale, and approval controls when needed."
  readability: "The task hero and live model stream have bounded compact heights, while the readable user/model conversation keeps the available central viewport."
  verification:
    ui_tests: "17 passed"
    agent_tests: "81 passed"
    build: passed
    syntax: "electron/main.cjs passed syntax check"
    diff_check: passed
  claim_boundary: "This addresses the recorded Chat/Build placement and hierarchy problem. It does not claim a new Maple inference or artifact-quality result."

chat_fullscreen_visual_polish_2026_08_16:
  status: implemented_verified
  design_reference: "/var/folders/70/37y2xd0n6vbf04fw178_zy9h0000gn/T/codex-clipboard-ceb2cc55-62fe-4518-b104-6f24131442e7.png"
  implementation_capture: "/tmp/hemlock-chat-polish-final-state.png"
  hierarchy: "The Chat/Code transcript is the visual center. The repeated objective is a one-line context disclosure, host activity and full trace are collapsed below the transcript, and the approved plan is a compact expandable dock above Work mode and the composer."
  readability: "User messages use a bounded centered measure and assistant output drops dashboard-card chrome while preserving visible reasoning channels and live model streaming. The compact task/context bands reclaim vertical space, and expanded host detail is bounded with internal scrolling so it cannot cover the transcript."
  privacy: "The live capture renders ~/Code/Hemlock rather than the user's macOS account name."
  verification:
    ui_tests: "17 passed"
    agent_tests: "81 passed"
    build: passed
    syntax: "electron/main.cjs passed syntax check"
    diff_check: passed
    design_qa: "design-qa.md result passed"
  claim_boundary: "This is a renderer/layout polish pass validated against the supplied screenshot and a live Electron capture. It does not claim a new Maple inference or artifact-quality result."

electron_host_ux_lane_b_2026_08_21:
  status: implemented_verified
  plan: docs/plans/2026-08-21-os-feel-ux-pass.md
  channels:
    dialog_confirm: "dialog.showMessageBox wrapper; tone-mapped danger/default buttons; returns boolean via mapConfirmChoice; empty message rejects honestly."
    notification_show: "Main-process Notification with whitespace-collapsed, length-clamped title (80) / body (240); darwin subtitle stamps app name; structured {shown,error} on failure."
    windows_list: "hemlock.window.list.v1 read-only pull of renderer-owned hemlock-os-windows-v2 localStorage snapshot; closed windows excluded, rows sorted by zOrder; honest unavailable payload when no renderer."
    preload: "mapleDesktop/hemlockAgent expose confirmDialog, notify, and windowsList."
  tests:
    host_ux_ipc: "dream-chat/electron/host_ux_ipc.test.cjs: 11 passed"
    electron_suite: "node --test dream-chat/electron/*.test.cjs: 95 passed"
    renderer_tests: "windowManager + styles tests: 16 passed"
    vite_build: "passed (dist rebuilt for HEMLOCK_PROD_UI flow)"
  claim_boundary: "IPC handlers and preload surfaces are verified against mocked Electron. No live desktop relaunch or real macOS notification delivery was exercised in this pass; consumers of notify() remain future work per plan."

tranche5_improvements_2026_08_22:
  status: implemented_verified
  plan: .hermes/plans/2026-08-22_120000-hemlockos-tranche5-improvements.md
  changes:
    maple_response_notifications:
      what: "Long Maple/provider chat completions (>30s, unfocused window) announce via workNotifier; structured-action never notifies; cancelled streams never announce or linger."
      files: "electron/main.cjs (hooks at runInference + runCliInference boundaries), work_notifications.cjs (trackChatResponseJob adapter), maple_notify.test.cjs (8 tests)"
      leak_guard: "Jobs settle (finish/cancel) BEFORE risky post-terminal work; Maple success-path receipt write is guarded with an event receipt on failure; cancel-after-finish is a no-op."
    evidence_ledger_grouping:
      what: "Chat evidence rail rows grouped by event-type prefix with counts + newest time (pure groupEvidence helper in src/evidenceLedger.js, 3 tests); stale thread.search comment corrected."
    launcher_stale_reap:
      what: "reap_stale_mlx_lm(port) kills non-child mlx_lm listeners on 8080 pre-Electron, warns on non-mlx_lm squatters, waits only after a kill; double-launch protected by PID-lock early return."
    copy_provenance:
      what: "Option-click copy on assistant messages includes [PROVIDER · time · thread] header + rawOutputRef via pure withProvenance helper (src/copyProvenance.js, 4 tests); plain click byte-identical."
    primer_command_center:
      what: "PrimerCard second mount at top of Command Center .workbench-main; shared dismissal state; no CSS/layout change."
    reopen_tips:
      what: "Settings 'Reopen getting-started tips' clears only the dismissed flag (clearPrimerDismissal), focuses Chat or falls back to Command Center."
    glossary_tooltips:
      what: "GlossaryHint component (7 usages, 5 terms): native title= hover kept; keyboard-focus and tap popovers with paper/sage/gold styling; Escape hides element-level without touching overlay ordering."
      fixes_after_review: "Click-after-focus no longer toggle-closes the popover (openedByFocus ref consumed by click); .glossary-hint position:relative so the popover anchors to the hint instead of .os-window (was clipped invisible). Both fixes empirically verified via CDP trusted input in real Chromium."
    narrator_consolidation:
      what: "Command Center copy-only dedup: ribbon NOW cell shows phase only, loop OBSERVATION cell points to Evidence card, work-note idle copy shortened, filler detail removed. No layout change."
    audit_viewport_convention:
      what: "docs/plans/2026-08-22-audit-viewport-convention.md pins detector audits at 1240x820 for comparable counts."
  verification:
    electron_suite: "node --test electron/*.test.cjs: 121 passed (113 baseline + 8 maple_notify)"
    renderer_suite: "node --test src/*.test.js: 26 passed (16 baseline + 3 evidenceLedger + 4 copyProvenance + 3 windowManager/styles additions)"
    vite_build: "passed"
    syntax: "main.cjs node --check passed; launcher zsh -n passed; main.jsx esbuild passed"
    reviews: "Every task passed independent spec+quality review except 3.1, which failed first review (2 empirically reproduced defects) and passed after direct fixes; 1.1 leak-path finding from quality review fixed with regression test."
  incident_note: "One fix subagent timed out after reverting uncommitted main.cjs hooks (git restore); hooks were re-applied directly by the orchestrator with the leak guard included. No other work was lost."
  claim_boundary: "All verification is host-side and deterministic (unit suites, builds, CDP DOM probes). Live Maple notification delivery, real macOS notification rendering, and a live desktop relaunch were not exercised in this pass."

tranche6_grounded_harness_2026_08_22:
  status: implemented_verified
  plan: .hermes/plans/2026-08-22_190000-tranche6-grounded-harness.md
  headline: "The Memory Garden now feeds the model: promoted lessons reach inference prompts with receipts and an off-switch."
  changes:
    grounded_memory_injection:
      what: "buildGroundedContext (electron/prompt_context.cjs) renders active-status SIPS records as a labeled system block [Hemlock memory · N records · verify-before-use], capped 1200 chars; injected into BOTH maple and CLI conversation inference via injectGroundedContext; context.injected event once per inference; structured-action path never injected; grounding chip in Chat status bar shows count and opens the popover/Memory Garden."
    thread_digest:
      what: "applyDigestCompaction/buildDigest/insertDigestBlock (electron/thread_digest.cjs): dropped middle history is summarized as metadata-derived digest lines (≤12, first sentences) re-entering the prompt inside the SAME char budget; thread.digest.created event once per inference (inside the provider lease)."
      compaction_bug_fix: "Pre-existing bug fixed in compactInferenceMessages: a trailing assistant reply after the latest user message was silently dropped even with huge budget; now kept with its chars budgeted. Regression test added to maple_runtime.test.cjs."
    context_meter:
      what: "telemetry.contextChars on both inference paths; composer model line gains .context-meter (used/budget % of 24k, gold at ≥80%), pure contextMeter helper."
    post_apply_verification_card:
      what: "Conversational code.apply success auto-runs the matched allowlisted verification profile once (runPostApplyVerification), pins hemlock.agent.verification.v1 on the task with honest skipped status when no profile matches; Chat renders a VERIFICATION card (passed sage / failed gold / skipped); code-repair autopilot actions excluded."
    completion_footnote:
      what: "Renderer-only host-labeled note pushed on completion after apply: 'Host receipt · change set applied · verification passed/failed'; dedupe keyed task+changeSet+ranAt."
    memory_fitness_loop:
      what: "memory_fitness.cjs (scoreRecord/rankForRecall/shouldAutoDemote/applyFeedback, 15 tests); sips_runtime.py additive sidecar feedback.jsonl + read_feedback/aggregate_feedback_counts/memory-feedback action; allowlisted memory.feedback command appends votes and auto-demotes via the EXISTING demote transition when irrelevant≥3∧>useful; grounding chip popover offers useful/not-relevant per injected record."
    comparison_lane:
      what: "comparison.run host command re-sends the thread's last user prompt verbatim to one alternate lane (read-only conversation inference, provider lease respected, one-in-flight guard reset in finally, pre-comparison task state restored on success AND failure); renderer lane picker popover + two-column verbatim compare view stacking under 800px."
  verification:
    electron_suite: "node --test electron/*.test.cjs: 172 passed (153 post-T5 baseline + prompt_context, thread_digest, verification_card, memory_feedback, comparison_lane, maple_runtime regression)"
    renderer_suite: "node --test src/*.test.js: 26 passed"
    build_and_syntax: "vite build passed; node --check main.cjs passed; esbuild main.jsx passed; py_compile sips_runtime.py passed"
    package_json: "verification_card.test.cjs added to test:agent script so CI cannot skip it"
    reviews: "Every task passed independent spec+quality review. Reviewers caught and parent fixed: missing thread_digest wiring, M1b dead wiring (memory.feedback never called runSipsRuntime — one-line fix), P1 blocked-state leak on failed maple comparisons (restore moved to catch)."
  known_minor_debt:
    - "inference.respond bypasses the intent path so it injects whatever agentTask.recall last held (stale-recall asymmetry, pre-existing pattern)"
    - "Maple vs CLI comparison lanes differ: maple gets grounded-context injection + digests, CLI does not — not apples-to-apples"
    - "task.comparison persists across turns without pinning the compared prompt text, so 'same prompt' can drift from the current column"
  claim_boundary: "All verification is deterministic host-side (unit suites, builds, syntax checks). Live cross-lane comparison runs, real recall injection against a populated Garden, and a live relaunch smoke were not exercised in this pass."

tranche7_seamless_loop_2026_08_22:
  status: implemented_verified
  plan: .hermes/plans/2026-08-22_210000-tranche7-seamless-loop.md
  headline: "The ask_user loop closes in place: Maple asks, you answer in Chat, the resumed loop streams visibly."
  changes:
    answer_in_place:
      what: "task_answer.cjs (assertAnswerable gate) + allowlisted task.answer command: validates waiting_for_user state, appends the answer to thread history via the same persistence as sendMessage, pins/clears task.question durably on the projection, then resumeTask continues the bounded loop. Renderer: MAPLE ASKS question card with inline answer box above the plan dock; StatusLamp reads ASKS YOU."
      streaming: "Live Model Stream panel now covers agent_action streams too (was model_text only) — resumed structured-action inference after an answer streams visibly instead of running silently (user requirement)."
    grantable_budgets:
      what: "clampBudgetOverrides (pure, steps 1-24, commands 1-40); plan.approve clamps + merges overrides into task.budget before approval so proposeNextAction enforcement honors grants; renderer stepper on the awaiting-approval card (defaults 8/12, overrides only when non-default) + live steps/commands usage line while running."
    debt_cleanup:
      what: "Comparison records pin promptText with a renderer PROMPT PINNED header + prompt-since-superseded hint; context symmetry achieved by wrapping ONLY the bare CLI lane with injectGroundedContext (maple injects internally; no double-injection, verified by call graph) with honest contextApplied flag; stale-recall guard: injectGroundedContext accepts refreshQuery used only at the inference.respond call site."
    live_eval_lane:
      what: "electron/tool_use_live.cjs (dual module/CLI): runs the 11-fixture tool-use benchmark against live Maple with shared wall-clock budget and skip-remaining-on-timeout; scores response quality only (respondedRate, validEnvelopeRate against fixture-derived allowlists); receipts to Application Support e2e/tool-use-live/; npm script eval:tool-use-live; explicit claimBoundary that task completion is never claimed. Injectable inferenceFn keeps tests offline."
  verification:
    electron_suite: "node --test electron/*.test.cjs: 190 passed (172 post-T6 + task_answer, clampBudgetOverrides, comparison prompt-pin, tool_use_live)"
    renderer_suite: "node --test src/*.test.js: 26 passed"
    build_and_syntax: "vite build passed; node --check main.cjs passed; esbuild main.jsx passed"
    reviews: "Batched review: S3 APPROVED, S4 APPROVED, S1 PASS pending one CSS fix (question-card row-shift lost the cascade to the later grid block — fixed with .is-asking scoped overrides) plus one S2 test-string mismatch (fixed). Reviewer also verified no maple double-injection and correct guard scoping."
  known_minor_debt:
    - "Inline ask_user model actions pin action.shortRationale as the question prompt (approximation, matches prior fallback)"
    - "Budget overrides apply before plan-status validation, so an explicit approve on a running task could mutate budget mid-run (allowlisted explicit, low risk)"
    - "Live eval lane not yet run against a live Maple server (receipt path verified by unit tests only)"
  claim_boundary: "All verification is deterministic host-side. The ask_user loop, budget grants, and comparison fixes are unit-verified; a live end-to-end ask-answer-resume session and a live tool-use eval run against Maple were not exercised in this pass."

tranche7_5_chat_reliability_2026_08_23:
  status: implemented_verified
  plan: .hermes/plans/2026-08-23_tranche75-chat-reliability.md
  trigger: "Real incident 2026-08-23: MLX Metal GPU-timeout (kIOGPUCommandBufferCallbackErrorTimeout) SIGABRT'd the maple server mid-stream 7 times; undici surfaced bare 'TypeError: terminated' which was not classified as a transport error, so the bounded auto-retry never fired."
  changes:
    transport_classification:
      what: "isMapleTransportError recognizes 'terminated'/premature-close (undici's error when the SSE server dies mid-stream) — the bounded restart+retry now fires on server-death kills. Committed separately as 76756e7 with honest empty-reply renderer copy distinguishing dropped-connection from token-limit."
    crash_loop_policy:
      what: "crash_policy.cjs (pure): recordCrash appends+prunes a rolling window; shouldRespawn blocks at ≥3 crashes in 10min (fails open on garbage). main.cjs: stopServer stamps child.mapleStopRequested so our own stops are not crashes; unexpected deaths (SIGABRT/SIGKILL) record + consult the policy — over budget skips respawn, sets serverState crashLooped/crashLoopReason, emits maple.crashloop.detected. maple.launch resets the loop. Recovery is lazy (next inference), so the gate sits at restartMapleRuntime entry."
    visible_recovery:
      what: "Transport retry emits maple.recovery (degraded) receipt; conciseAgentNote renders 'Connection to {provider} dropped; the runtime restarted and your prompt is being re-run (attempt N)' in HOST ACTIVITY."
    rerun_on_dead_reply:
      what: "Dead replies (streamDied) render honest copy + a Re-run-this-prompt button reusing retryLastMessage (same helper as per-message retry); token-limit branch unchanged."
    ipc_clone_errors_cleared:
      what: "All 6 'object could not be cloned' errors in launch.log date to Aug 15 (before the stability-pass fix); zero occurrences since. Historical — no live bug."
    mlx_upgrade_0321:
      what: "Root cause of the GPU-timeout SIGABRTs identified upstream: mlx <0.32.0 threw Metal CommandBuffer errors from the completion handler (uncatchable → abort). PR #3523 (in 0.32.0) defers them to safe checkpoints so the server can catch and continue. Upgraded the Hemlock runtime mlx 0.31.2 → 0.32.1; 8 consecutive generations with zero aborts (previously crashing every few requests). setup.py documents the recommendation."
  verification:
    electron_suite: "node --test electron/*.test.cjs: 200 passed (190 + 10 crash_policy incl. source wiring guards)"
    renderer_suite: "node --test src/*.test.js: 26 passed"
    build_and_syntax: "vite build passed; node --check main.cjs passed; esbuild main.jsx passed"
    reviews: "Batched review APPROVED. Reviewer verified: stopServer SIGKILL-fallback classifies as our-stop (no false crash), crashLooped reaches renderer via getAgentState server snapshot, maple.launch reset path correct, R2 fires once per retry, R3 reuses retryLastMessage with no duplicated send logic."
  known_exemption:
    - "recoverBaseServer (dream-run failure recovery) and ensureBaseInference (SIPS baseline) call stopServer+startServer directly, bypassing the shouldRespawn gate — INTENTIONAL: both are user-initiated training flows, not auto-retry loops; thrash risk accepted and documented."
  claim_boundary: "All verification is deterministic host-side (unit suites, builds, log analysis). The crash-loop gate, visible recovery note, and re-run button were not exercised against a live mid-generation server kill in this pass; the GPU-timeout itself is upstream mlx/Metal."

tranche8_stability_speed_2026_08_23:
  status: implemented_verified
  plan: .hermes/plans/2026-08-23_tranche8-stability-speed.md
  changes:
    cache_telemetry_sizing:
      what: "NEW prompt_cache_stats.cjs (pure hitRatio helper, 6 tests); all three inference telemetry blocks emit cachedTokens + cacheHitRatio from usage.prompt_tokens_details.cached_tokens; Host telemetry shows 'cache N%'; prompt cache defaults raised to size 12 / 768MB after live verification (768M accepted; repeat request measured 94% hit). Env clamp ceiling fixed 10→16 so operators aren't clamped below the default."
    dead_flag_cleanup:
      what: "Removed --ngram-draft/--ngram-depth and their env consts — architecturally incompatible with Maple (SWA-512 RotatingKVCache defeats the ngram verifier's cache rewind; mlx_lm/speculative.py fallback). FlashHead remains the decode lever. Launcher audit read-only."
    first_token_stall_watchdog:
      what: "NEW stream_watchdog.cjs (pure, 4 tests); Maple SSE path arms a 45s no-first-byte timer per attempt — abort with MAPLE_FIRST_TOKEN_STALL routes into the bounded transport retry instead of hanging the full 10-minute inference timeout. Timer disarms on first byte and in finally."
    metal_error_surface:
      what: "With mlx 0.32.1 Metal errors arrive as HTTP 500 (server survives). isMapleTransportError now classifies 500s whose body names Metal/CommandBuffer/GPU as transient → bounded retry. Renderer empty-reply tiers rewired to reachable data (streamStatus + streamStopReason captured from terminal stream frames — reviewer found the previous streamDied/errorCode flags were never populated, dead since T7.5); three honest copies: GPU error / connection dropped / token-limit."
  review:
    verdict: "S1 APPROVED (clamp gap fixed), S2 APPROVED, S3 APPROVED, S4 gaps fixed by parent after review (streamStopReason wiring + clamp ceiling)."
    gates: "electron 210/210, renderer 26/26, vite build ok, node --check ok, esbuild ok"
  claim_boundary: "Deterministic host-side verification only. The stall watchdog was not exercised against a live SIGSTOP'd server; cache-hit rendering not yet seen live in Chat (needs app relaunch)."

fix_partial_output_and_fresh_context_2026_08_24:
  status: implemented_verified
  trigger: "User incident: a short-story request streamed 17,632 chars over ~90s (verified in model-output receipt model_text-1787545277637) but the stream was cut before finish_reason; Hemlock's cancel path discarded everything — no assistant message persisted. Separately, five explicit-story refusals stayed in thread history and Maple's reasoning channel re-litigated them every turn ('the user is testing boundaries again'), poisoning all later prompts."
  changes:
    keep_partial_output:
      what: "Cancelled/interrupted inference now persists streamed content as an assistant message with partial:true + stopReason in the thread (thread_manager.appendConversation carries the fields); renderer shows 'Interrupted — reply kept up to the cut' with gold border and preserves partial/stopReason across history loads."
    fresh_context:
      what: "NEW conversation.reset host command + threadManager.resetConversation (archives conversation to <file>.<ts>-archive.jsonl, clears it, keeps thread identity/checkpoints/receipts). Allowlisted as explicit-approval; palette action 'Fresh context'; renderer clears messages and shows an archived-count host footnote."
  verification:
    gates: "electron 210/210, renderer 26/26, vite build ok, node --check ok, esbuild ok, thread_manager suite 5/5"
    live_evidence: "Incident diagnosed from durable receipts: raw SSE payload had 4217 chunks / zero finish_reason; thread jsonl confirmed poisoned-history reasoning verbatim."
  claim_boundary: "Partial-output persistence and reset verified deterministically (suites/build); neither exercised live end-to-end in a running app yet."

fix_toks_artifactid_layout_2026_08_24:
  status: implemented_verified
  trigger: "User reports: (1) tok/s renders '—' in Host telemetry; (2) artifact build blocked with 'Stop reason: deterministic-input: Invalid artifact artifact ID.'; (3) plan card crowds/overlaps the composer in Build mode."
  changes:
    toks_per_second:
      what: "Root cause: mlx_lm only emits a usage chunk when stream_options.include_usage is set — receipts showed 660 chunks, zero carrying usage, so completionTokens was always null. Added stream_options:{include_usage:true} to both Maple SSE requests; added honest fallback estimating completionTokens from streamed content+reasoning chars (÷4) flagged completionTokensApproximate; renderer prefixes '~' when approximate."
    artifact_id_sanitization:
      what: "Root cause: the model parrots plan-step refs ('artifact://manifest') as input.artifactId; orchestrator only filled the id when falsy, so garbage ids reached safeSegment and threw. executeCommandInput now deletes any artifactId failing ^[A-Za-z0-9][A-Za-z0-9._-]{0,100}$ so the existing prior-artifact fallback fills the task's real id."
    work_rail_layout:
      what: "Plan card in the work rail had flex 0 0 auto + 42vh max-height inside an overflow:hidden rail — tall plans squeezed/overlapped the composer. Rail now scrolls vertically (overflow-y auto); plan card is flex 0 1 auto capped at 34vh/340px so it shrinks instead of pushing the composer out."
  verification:
    gates: "electron 210/210, renderer 26/26, vite build ok, node --check ok, esbuild ok"
  claim_boundary: "Deterministic verification only; tok/s and layout fixes need a live chat session to confirm visually."

fix_remove_escalation_2026_08_24:
  status: implemented_verified
  trigger: "User directive: Maple-Preview failed and Hemlock suggested escalating to Codex/Claude — remove the escalation mechanism entirely; HemlockOS runs only the selected model lane."
  changes:
    escalation_removed:
      what: "Removed both provider-escalation suggestion sites (orchestrator blockTask + recordCliInferenceFailure), the renderer auto-switch on suggestion.accept, the task.escalate-provider command, and its allowlist entry. A Maple failure now surfaces solely as an honest blocked task with its reason."
  verification:
    gates: "electron 210/210, renderer 26/26, vite build ok, node --check ok, esbuild ok; zero 'escalate' references outside policy comments"
  claim_boundary: "Deterministic verification only."

tranche9_hardening_2026_08_24:
  status: implemented_verified
  plan: .hermes/plans/2026-08-24_tranche9-hardening.md
  changes:
    readiness_hardening:
      what: "NEW readiness_probe.cjs (backoff min(2000,250*2^attempt), deadline reason, classifyHealthFailure); readiness deadline errors now name the last failure classification; child-exit during readiness fails fast with signal; model path validated BEFORE spawn (config.json + safetensors required) — kills the LFM2.5 404 class."
    error_taxonomy:
      what: "NEW error_taxonomy.cjs classifyInferenceError → {kind, retryable, userMessage} across 7 kinds (transport-death/stall/gpu-server-error/cancelled-by-user/model-invalid/server-not-ready/unknown); isMapleTransportError delegates to it with identical verdicts (maple_runtime tests unmodified-green). agent:cancel handler returns a plain-data snapshot if structured-clone fails."
    artifact_flow_tests:
      what: "NEW artifact_flow.test.cjs (6 tests): garbage artifactId stripping ×5 incl. numeric 42, author-without-create host scaffold, bounded repair loop with blocked-then-passed inspect sequence, clamp unit. EXPOSED GAP found and fixed: numeric artifactIds passed the sanitizer via String() coercion — sanitizer now rejects non-strings."
    renderer_hygiene:
      what: "TERMINAL_STREAM_STATUSES includes 'restarting' so restarting streams don't leave stale LIVE panels; re-run buttons disabled while thinking; error banner auto-dismisses after 12s (manual dismiss clears timer); .partial-note/.chat-error flex fixes."
    stop_button:
      what: "Composer Stop button appears only while streaming (isThinking or live model_text stream); calls agent:stream-cancel (stream-scoped, preserves the task) with agent:cancel fallback; wired to pre-existing dead plumbing from b165526; cancelled stream flows into partial-persist; draft preserved; gold-styled, cancelBusy-guarded."
  review:
    note: "Batched reviewer timed out mid-review (17 calls, no findings written); parent performed direct verification of highest-risk items instead: stream-cancel handler (no duplicate registration, aborts non-terminal model_text, T8-F1 compatible), preload bridge both paths, H1 validation placement before spawn, H5 renderer guard logic. Skipped-test follow-up: numeric-ID gap found by H3's skipped test was fixed in product code and the test unskipped."
  verification:
    gates: "electron 236/236 (0 skipped), renderer 26/26, vite build ok, node --check main/orchestrator/maple_runtime ok, esbuild ok"
  claim_boundary: "Deterministic verification only. Stop button and readiness diagnostics not yet exercised against a live wedged server."


tranche10_chat_build_stability_2026_08_24:
  status: implemented_verified
  plan: .hermes/plans/2026-08-24_tranche10-chat-build-stability-ux.md
  commit: e707da7
  changes:
    coalesced_stream_updates:
      what: "createFrameCoalescer (streamStore.js) buffers stream frames, drains on rAF or 50ms cap (background-tab stall fix), terminal frames drain synchronously; main.jsx subscriber pushes frames through the identical reducer at flush — ordering/dedupe/verbatim semantics unchanged; shared createScheduler fixes prior rAF leak on dispose."
    composer_untrap_guards:
      what: "startDream pre-flight updateTask failure no longer strands isDreaming=true; browser-path sendMessage pre-flight failure clears isThinking + timer and emits prompt.failed. Both previously left the composer permanently disabled."
    plan_approval_guards:
      what: "approvePlan(taskId, planId, budgetOverrides) rejects non-waiting_for_approval task states with an honest blocked result (no mutation); clamps overrides via existing pure clampBudgetOverrides inside the orchestrator so direct callers are safe too."
    queue_duplicate_rejection:
      what: "agent_queue enqueue rejects an identical pending objective (status duplicate, points at existing entry); active-task steering unaffected; FIFO order untouched."
  verification:
    gates: "electron 241/241 (236 baseline + 5 new), renderer 30/30 (26 baseline + 4 new), vite build ok, node --check ok, esbuild ok"
    integration: "Both lanes' diffs reviewed by parent before single tranche commit; renderer gates re-run independently after host changes."
  claim_boundary: "Deterministic verification only. Coalescing latency improvement and untrap guards not yet exercised in a live streaming session; duplicate rejection semantics not yet exercised live. Deferred follow-ups: host stream/projection perf audit (per-frame stringify, unbounded arrays, listener leaks), error-taxonomy gap sweep for uncovered user-visible errors."

tranche11_chat_build_polish_2026_08_24:
  status: implemented_verified
  plan: .hermes/plans/2026-08-24_tranche11-chat-build-polish-hardening.md
  commit: 125d36b
  base: e707da7 (first tranche under delegation.child_timeout_seconds=1800 — both lanes landed in one run, no re-dispatch)
  changes:
    host_stream_perf:
      what: "checkpointStream JSON.stringify'd the full channel map on EVERY SSE chunk before the throttle check (O(total-output²) per stream); replaced with pure shouldCheckpointStream predicate + incremental stream.channelBytes accounting; serialization now happens once on actual checkpoints. streamRing leaked up to 240 retained frames per finished stream (write-only, never read) — deleted in finishStream. agentEventIds dedup Set grew forever while agentEvents capped at 160 — bounded to a 4096 insertion-order window."
    error_taxonomy_gaps:
      what: "New kinds: endpoint-not-found (HTTP 404, non-retryable), server-error (non-GPU 5xx, retryable), action-envelope-invalid (INVALID/EMPTY_ACTION_OUTPUT, non-retryable — repair loop already exhausted), transport-death extended to EHOSTUNREACH/ENETUNREACH/EAI_AGAIN/getaddrinfo. Retry behavior unchanged (maple_runtime keeps its narrow kind set)."
    renderer_memoization:
      what: "TranscriptMessageRow = memo() at module scope with primitive-only props + ref-backed stable onRetry; coalesced flushes re-render only the advancing row. channelProviderName hoisted; reasoning toggle became a prop."
    streaming_prose:
      what: "Fixed real bug: empty bubble mid-stream flashed terminal 'finished without text' copy — now shows 'Waiting for first output…' with caret. 72ch measure + text-wrap pretty on streamed content; caret softened to opacity pulse with prefers-reduced-motion fallback; gold LIVE dot on open reasoning channels."
    contrast_fixes:
      what: "Reasoning body #7d7460 was 4.32:1 over tinted bg → #6b6350 (5.56:1). Skipped verification card opacity:.82 dimmed kicker to 4.09:1 → explicit muted colors (#5f7161 5.13:1) with regression test banning opacity. Failed tone #956d23 passes at 4.60:1 (thin headroom, left as-is). Plan-card hierarchy reordered objective → steps → usage counters → stepper/actions; budget usage 10px DM Mono tabular."
  investigated_no_change:
    - "stream_dispatcher coalescer clean; SSE parser buffers correctly"
    - "agent_kernel event spine already bounded everywhere (ops -80, plans -40, actions/observations/candidates -120, episodes -40)"
    - "no listener leaks — all .on() attach to short-lived children or app-level once-handlers"
    - "repo.inspect caching skipped: dispatch lives in main.cjs command layer, no small call-site change exists"
  verification:
    gates: "electron 249/249 (+8), renderer 33/33 (+3), vite build ok, node --check ok, esbuild ok; all re-run by parent after lane integration"
  claim_boundary: "Deterministic/static verification only (contrast math, source tests, builds). Memo skip behavior and O(n²) fix not yet observed against a live Maple stream; live relaunch smoke still outstanding from T10."

iconography_functionality_2026_09_19:
  status: implemented_verified
  execution: "Solo implementation; no subagents, as requested."
  scope: "Semantic iconography, state-aware window controls, accessible window actions, center/reset sizing, persistent Show desktop/restore, shortcut guide, exact-text clipboard feedback and regression coverage."
  icon_registry: "52 named app/action glyphs plus an explicit unknown fallback; real registry rendered at 16/20/28px in the evidence sheet."
  functionality:
    menus: "Shared titlebar/dock menu, 8 actions, keyboard navigation/type selection/Escape, focus restoration and viewport clamping."
    desktop: "Persistent reversible minimization snapshot; preserves maximized state, preexisting minimized windows, current session drafts, and excludes closed windows from restoration."
    shortcuts: "Visible guide and app launchers; F1 help, Cmd+Option+D desktop, Cmd+Option+W preference-aware close; modal shortcut isolation."
    clipboard: "Exact whitespace-preserving copy, Option provenance, pending/success/error feedback and recovery; test double exercises rejected clipboard without touching user clipboard."
  verification:
    renderer_components: "105/105 passed, 0 skipped"
    controls_browser: "25/25 passed"
    existing_browser_smoke: "50/50 passed"
    existing_audit_regressions: "19/19 passed"
    host: "257/257 passed"
    real_electron: "20/20 passed with isolated data and production bundle; no renderer exceptions"
    production_build: "npm run build passed; git diff --check clean"
    design_scan: "New/changed control markup scan returned []"
  evidence: work/iconography-controls
  documentation: docs/gui-workspace.md
  claim_boundary: "Clipboard UI tested against an explicit isolated test double; no model inference/training or real conversation modifications."

gui_refinement_2026_09_19:
  followup_assessment:
    status: implemented_verified
    request: "Impeccable critique and technical audit, followed by broad implementation and verification; user explicitly does not want an assessment-only deliverable."
    target: dream-chat/src/main.jsx
    baseline_renderer_tests: "68/68 passed"
    assessment_a: work/gui-assessment-a
    assessment_b: work/gui-assessment-b
    baseline_design_score: "25/40, independent design review"
    baseline_audit_score: "11/20, independent technical audit"
    confirmed_gaps: "Compact inspector hit-test obstruction, transcript height budget, idle cockpit hierarchy, actual contrast failures, repetitive navigation/ungrouped Settings, incomplete theme tokens and illustrative Dream chart."
    archived_critique: .impeccable/critique/2026-09-20T01-37-46Z__dream-chat-src-main-jsx.md
    implementation:
      chat: "Build no longer auto-opens evidence; compact inspector occupies only the transcript region with focus transfer, Escape, outside-pointer dismissal and opener restoration. Composer is compact/growing; model context/help are optional."
      workspace: "Task-first idle home with genuine readiness/recent-thread data; active/blocked/approval cockpit retained. Four task-based Settings sections with preserved callbacks and close-warning preference."
      navigation: "Four frequent dock apps plus open/minimized windows and All apps; grouped library; stable Cmd+1..0 app shortcuts; explicit tile/fill dock menu; stacked preview scroll-to-window."
      integrity: "Removed illustrative Dream loss chart; corrected runtime provenance, missing-context labels, invalid ARIA role, paper status contrast and workbench theme tokens; essential affected metadata >=10px."
    measurements:
      minimum_chat_transcript_px: "134 before, 231 after"
      minimum_chat_composer_px: "209 before, 120 after"
      compact_760_transcript_px: "100 before, 180 after; 180 retained under 200% text-only scaling"
      thread_state_contrast: "paper 5.71:1; understory 6.48:1"
    verification:
      renderer_components: "92/92 passed, 0 skipped; includes isolated browser component tests"
      host: "257/257 passed"
      browser_smoke: "50/50 passed"
      audit_regressions: "19/19 passed; hit-testing/focus/compact sizing/text scaling/contrast/navigation"
      real_electron: "13/13 passed; built production bundle, isolated data, real preload bridge"
      build: "npm run build passed; git diff --check clean"
    evidence: work/gui-audit-fixed
    claim_boundary: "Measured fixes verified; baseline critique/audit scores are not relabeled as post-fix scores. No live inference/training or full screen-reader certification; no user conversation changes, commit or push."
  status: implemented_verified
  product_brief: "Impeccable init refreshed root PRODUCT.md after explicit user confirmation: OS-workspace priorities, flexible layout/navigation, current capabilities, accessibility and evidence. DESIGN.md and code-first workflow default unchanged."
  scope: "OS-style workspace/navigation, managed Command Center, window-sized responsive layouts, transcript-first chat, readable controls, predictable resizing and persistent close-warning preferences."
  changes:
    navigation: "Searchable Windows overview with open/minimized/focused states, native Cmd+Shift+O, dock arrow navigation, palette focus restoration and scroll-following selection."
    windowing: "All eight resize handles support pointer and keyboard; resizing anchors the opposite edge; drag snapping previews before release; minimize restores prior maximize state; focus hands off after close/minimize."
    chat: "Conditional React hooks moved out of renderChat; composer moved below transcript; wide inspector becomes a dismissible drawer in compact windows; IME-safe Enter handling."
    layout: "Container-query panels, unclipped native 760px floating workspace, real Command Center titlebar, shared legible theme tokens, comfortable chat sizing and restored-session minimum migration."
    preferences: "Never show this again applies only to window-close prompts; persisted and reversible in Settings."
  verification:
    host_tests: "257/257 passed"
    renderer_tests: "68/68 passed, including real WindowFrame component handlers"
    browser_smoke: "49/49 passed at 1440x900, 1024x768, 760x620, and 390x844; no renderer exceptions"
    electron_smoke: "7/7 passed against production bundle with real preload bridge and temporary application data; no renderer exceptions"
    production_build: "npm run build passed; git diff --check clean"
    design_scan: "Single Impeccable scan found five legacy accent-stripe warnings; simplified those borders/shadows."
  documentation: docs/gui-workspace.md
  evidence: work/gui-refresh-2026-09-19
  claim_boundary: "GUI and window behavior exercised in Chrome and real Electron. No live model inference or training performed; user's saved conversations untouched. No commit or push."

tranche12_build_loop_hotfix_2026_08_24:
  status: implemented_verified
  trigger: "User-reported live incident: Build mode looped re-proposing completed commands, plan card covered chat, streamed output rendered ABOVE sent messages, no artifact ever reached the viewer."
  root_causes:
    plan_mutation_loop: "adaptPlanForCommand INSERTED a new plan step on every adaptive selection of an already-completed command — the plan grew [repo-map, repo-map, ...] each round, pushing artifact.author permanently out of reach (projection.jsonl: repo-map x24, create/author x20 all-time, zero previewable artifacts)."
    author_failures: "artifact.author failed on empty input / sha256-as-id because executeCommand never injected taskId (registry resolved under wrong task) and missing-artifact threw before any fallback."
    ui_ordering: "chat-live-stream panel rendered BEFORE messages.map in chat-surface; plan card shared grid cell with chat-work-rail (layered on top)."
  changes:
    anti_repeat_guard: "completedTaskCommands + resolveProgressCommand pure helpers; proposeNextAction redirects a repeat proposal to the plan's next incomplete step deterministically, emits action.redirected host note; adaptPlanForCommand refuses to insert duplicates of completed commands while open planned work remains."
    author_bulletproofing: "ensure-create-if-missing before authoring; registry throws retry once through host scaffold fallback; taskId injected at executeCommand call site; next-action prompt now states 'Completed already (do NOT repeat): …' and 'Plan progress: step N of M'."
    renderer_ordering: "live-stream panel moved below transcript with border-top separator; plan card moved to its own grid row in column 1 under the transcript (row 5), work rail untouched in column 2; message-order invariant verified (append-only, streamId-keyed)."
    test_contract_fixes: "mock executeCommands mirror production artifactCommandReceipt shape (artifactId field); assertions updated to assert executed behavior rather than raw preserved proposal inputs."
  verification:
    gates: "electron 257/257 (+8 net), renderer 34/34 (+1), vite build ok, node --check ok, esbuild ok; debug probes stripped post-debug"
    commit: e1fbefe
  claim_boundary: "Deterministic verification only until live smoke completes; loop fix exercised via regression test replaying the incident pattern (repeat proposals redirected once, plan stable at 2 steps), not yet against live Maple."
understory_grove_and_compact_choice_2026_09_20:
  status: implemented_verified
  trigger: "User asked to continue the uncommitted GUI tranche, test OS feel for both agent and human friendliness, make Maple work better, and explore a 3D world where local models exist and learn."
  changes:
    grove_surface: "New Understory Grove window: lazy-loaded three.js night-forest scene where each local model is a hemlock sized by real parameter scale (Maple 20B-A1B, LFM2.5-8B, two draft saplings). groveBindings.js is a pure event-spine mapper — inference events light fireflies at real tok/s, dream.* grows luminous graft whorls, memory.* plants seeds/blooms, sips.* raises wind, failures pulse ember. Paper bindings rail keeps every animated claim inspectable (real directory, params, dtype, last real event); external provider lanes are never grove residents."
    grove_wiring: "New grove icon glyph, window definition (820x560 preferred), World nav group, Command Center rail-footer link, palette/overview/guide integration. Grove sits past the ten digit slots so existing Cmd+1..0 chords never moved; kbd hint hidden for index >=10."
    maple_compact_choice: "Structured-action contract shrank from a 10-field envelope (whose id/taskId/step/approval/status the host overwrote anyway) to a 4-field choice {kind,commandId,input,shortRationale}. normalizeCompactChoice pins schema on choice-shaped objects so compliant compact output is first-class (parseStatus compact-choice); full envelopes still validate for compatibility. coerceActionPayload now also covers terminal kinds."
    prompt_cache_ordering: "Structured-action system prompt is now fully static — volatile planned command, completed list, plan progress, allowedNextCommands and input contract moved into the request body (actionRequest.progress/allowedNextCommands). Static prefix maximizes prompt-cache reuse; the existing cacheHitRatio telemetry makes the win measurable."
    smoke_coverage: "Electron GUI smoke gained 3 grove checks (window opens, WebGL canvas mounts, roster renders). favicon added (kills the 404 console error seen in every prior browser session)."
  verification:
    host_tests: "259/259 passed via node --test electron/*.test.cjs (up from 257; +2 compact-choice tests, prompt-progress assertions updated)"
    renderer_tests: "111/114 passed, 0 failed, 3 skipped (includes 9 new groveBindings tests)"
    real_electron: "23/23 passed with production bundle, isolated data, real preload bridge — grove opens with live WebGL and honest roster; no renderer exceptions"
    production_build: "npm run build passed; three.js code-splits into its own lazy chunk (736 kB, fetched only when the grove opens); git diff --check clean"
  evidence: work/understory-grove-2026-09-20
  claim_boundary: "Bindings map real session events only; the scene is ambient, never a metrics claim — receipts remain source of truth. Compact-choice reliability and cache-hit improvement are deterministic-contract changes verified by host tests; live Maple inference quality under the new contract is unclaimed until the Maple server lane runs (server was not running during this session). No commit or push."
world_experiments_and_dream_dataset_2026_09_20:
  status: implemented_verified
  trigger: "User clarified the world idea: Maple lives in the understory, runs physics experiments, learns, and adds to datasets; keep improving Maple agenticness and Dream."
  changes:
    physics_sandbox: "electron/physics_sandbox.cjs — pure deterministic core. Six bounded experiments (projectile, pendulum, spring, orbit, collision, terminal) with per-parameter clamp bounds and undisturbed-case defaults (drag/damping default 0, never range midpoint). Each run returns measured values beside closed-form theory plus a divergence map, so findings are verifiable. Fixed-step integration; orbit uses coarser dt with real period detection. No IO, no clock, no randomness in the sims."
    experiment_commands: "Three allowlisted agent commands — experiment.run (capability verify, auto, model-selectable), experiment.note (capability write, appends a hemlock.world.finding.v1 row to sipsDir/experiment-dataset.jsonl citing a real run receipt; model supplies only the claim, host attaches measured/theory verbatim), experiment.dataset (read). Per-run receipts at sipsDir/experiments/<deterministic-spec-hash>.json. experiment.completed / experiment.note.recorded events carry evidenceRefs."
    dream_feed: "runDream now appends the last 24 experiment findings to examples (messages/metadata shape dream_train.py already accepts) alongside facts/conversation — every Dream path (training.start, dream, SIPS-nested) inherits world data. dream.started datasetRows counts them."
    experiment_intent: "interaction_modes classifies experiment/physics/simulate/projectile/pendulum/orbit/gravity/spring/collision intents; defaultPlanSteps plans [experiment.run, experiment.note, answer]. COMMAND_EVIDENCE + actionInputContract + boundedActionInput cover the new commands; Maple can also select them adaptively (verify/write are SAFE_ADAPTIVE_CAPABILITIES)."
    grove_habitat: "Maple now has a lab bench at its tree. experiment.* events bind to a new effect: experiment.completed fires a teal lab flash + ring, experiment.note.recorded a seed ring, failures ember. Legend gained an experiments dot; maple's learnsFrom includes experiment. Experiments never grow grafts — dream growth stays bound to dream.* events only."
    dream_lab_dataset_card: "Dream Lab shows a 'World findings → Dream dataset' section: row count, latest finding, and per-experiment run buttons that call experiment.run through the real command bridge."
    renderer_perf: "EventRow memoized; latestReceiptEvent memoized on events; previewDocument srcDoc hoisted to a useMemo keyed on the artifact object so MB-scale artifact sources no longer re-concatenate on every event/stream render."
  verification:
    host_tests: "269/269 passed via npm run test:agent (was 259; +10: physics_sandbox suite + experiment intent cases)"
    renderer_tests: "113/116 passed, 0 failed, 3 skipped (+2 groveBindings experiment tests)"
    real_electron: "27/27 passed — new checks prove experiment.run returns a receipt and a measured pendulum period within 5% of theory, experiment.note writes a provenance-tagged dataset row, and experiment.dataset reads it back through the real preload bridge"
    production_build: "npm run build passed; git diff --check clean"
  evidence: work/understory-grove-2026-09-20
  claim_boundary: "The world loop is verified end-to-end deterministically in real Electron with isolated app data — run, receipt, finding, dataset readback, Grove bindings. Dream ingestion is verified at the input-shape level (examples merged into runDream payload); a live Dream training run on experiment rows and live Maple experiment planning are unclaimed until the Maple server lane runs. No commit or push."
maple_loop_improvements_2026_09_20:
  status: implemented_verified
  trigger: "Continue improving Maple on performance, learning, and agenticness."
  changes:
    parameterized_anti_repeat: "The T12 anti-repeat guard deduped on commandId only, which would have blocked legitimate experiment sweeps (pendulum at length 1 then 2 — same command, genuinely new evidence). Repeats are now keyed on commandId + normalized input digest for parameterized commands (experiment.*, file/context read+search, recall, receipts, verify, code/artifact inspect); non-parameterized commands still repeat on commandId alone so junk-parameter evasion can't bypass the guard."
    world_findings_in_context: "The structured-action prompt's volatile progress block now carries worldFindings — the last 3 experiment findings (experiment, claim, divergence %) via a worldContext dep wired in main.cjs. Maple's next bounded action sees what it already learned, so follow-up experiments can build on prior results instead of proposing blind."
    findings_to_memory_garden: "experiment.note also mirrors each finding into the agent kernel's candidate review queue (kind experiment, confidence scaled by divergence), deduped by fingerprint — world evidence is inspectable in Memory Garden alongside other candidates. Dataset row stays authoritative; the mirror is best-effort."
    inference_latency_metrics: "bumpAgentMetrics now accumulates inferenceLatencyMs on both the structured-action and conversation inference paths, so agentTask.metrics carries real total latency next to inferenceCalls — previously per-call latency lived only in events."
    dream_readiness_hint: "Dream Lab's world-dataset card surfaces 'Ready for a Dream run' once 6+ findings exist."
    choices_array_recovery: "normalizeCompactChoice takes the first well-formed entry from a {'choices':[...]} response instead of letting the batch silently degrade to the planned step (records __choiceCount)."
  verification:
    host_tests: "271/271 passed via npm run test:agent (+2: parameterized-repeat cases incl. junk-input evasion, choices-array first-choice)"
    renderer_tests: "113/116 passed, 0 failed, 3 skipped"
    production_build: "npm run build passed; git diff --check clean"
    electron_smoke: "deferred this round — the machine hit an application-memory ceiling mid-session (two orphaned mcp-playwright Chrome instances at ~15 GB each were reclaimed); host/UI/build gates cover the touched seams and the prior 27/27 smoke covered the command path"
  evidence: work/understory-grove-2026-09-20
  claim_boundary: "All changes are deterministic-contract and host-side; verified by host/UI tests and build. No live Maple inference this session. The anti-repeat change is exercised at unit level including the experiment-sweep case; its behavior inside a live multi-step plan is unclaimed until the Maple lane runs. No commit or push."
grove_humanoid_replay_2026_09_20:
  status: implemented_verified
  trigger: "Make the learning environment substantially more live and real — humanoid residents, not trees; Maple runs experiments and learns in it."
  changes:
    humanoid_residents: "Grove residents are now hooded low-poly figures (robe, mantle, cowled head, chest heartlight), height-scaled by real parameter size — Maple ≈2.3m at its lab bench, drafts ≈0.9m. Breathing, head sway, and activity-linked rim/heart emissive replace tree sway; dream grafts are now orbiting halo whorls. Forest environment (treeline ring, moss, stars) kept."
    evidence_replay: "physics_sandbox now emits a bounded (≤96pt) sampled trail on every run — real integration samples with axis labels per experiment (x/y, theta, xa/xb, y/v). Trails ride the receipt AND the experiment.completed event payload. groveBindings queues the newest 6 trail-bearing runs as bound.experiments; groveScene replays the actual measured path on a lab rig — pendulum arm swinging real theta, projectile/orbit path on a normalized plane, spring/collision motes on a rail, terminal fall. Playback paced by the trail's real t; ribbons reveal then fade; newest replay wins; reduced-motion draws instantly. The world animates measured evidence, not symbols."
    lab_readout: "Bindings rail gained a LAB REPLAY card: experiment name, sample count, worst divergence %, age — every animated claim stays inspectable."
  verification:
    host_tests: "273/273 passed (+2 trail suites: bounded axes/bounds/determinism/time-order)"
    renderer_tests: "115/118 passed, 0 failed, 3 skipped (+2 replay-queue tests)"
    production_build: "npx vite build clean; git diff --check clean"
    electron_smoke: "29/29 real-Electron checks passed — new: receipt carries ≤96pt trail over the real bridge, lab readout shows the newest run's experiment"
    visual: "work/understory-grove-2026-09-20 + /tmp/grove-humanoid.png — humanoid figures render in browser preview; Electron grove verified via DOM assertions"
  evidence: work/understory-grove-2026-09-20
  claim_boundary: "Trail capture, event plumbing, replay queue, and scene replay are verified deterministically and in real Electron. Replay *visuals* in live Electron are asserted via DOM (readout + trail payload), not screenshot-verified mid-animation. No commit or push. QA scripts now close browsers in finally blocks."
grove_active_lab_blockers_2026_09_20:
  status: implemented_verified
  trigger: "Make the grove less ambient — Maple actively experimenting — and fix blockers Maple hits doing artifacts."
  changes:
    compact_rationale_fix: "REAL BLOCKER from the user's live session: Maple emits {'kind':'tool','commandId':'artifact.author','input':{}} — a valid compact choice — but schema-pinning before validation made the missing shortRationale throw, and coerceActionPayload early-returns once schema is pinned, so every such action burned a full ~6min repair inference or blocked. normalizeCompactChoice now synthesizes shortRationale/input host-side (host owns narration anyway). Verified: no action.parse.failed events in test."
    preview_session_recovery: "REAL BLOCKER: task.blocked with 'Preview session was not found: undefined' — artifact.preview.inspect ran adaptively with no prior preview.open, and previewSessions.get(undefined) threw. Inspect now resolves the newest session, or auto-opens one from the task's artifact (event notes the recovery); interact/stop get the same newest-session fallback."
    maple_locomotion: "experiment.started event now emits before the host sim runs; bindings pulse lab-work. The figure physically walks to the bench on work events, faces the rig, raises an arm during replay (shoulder-pivot arms), walking bob while moving, drifts home when quiet. Camera lerps to the lab during replay and returns. All still event-bound — no invented activity."
  verification:
    host_tests: "274/274 passed (+1: rationale-less compact choice asserts zero parse failures)"
    renderer_tests: "115/118 passed, 0 failed, 3 skipped (lab-work pulse ordering)"
    production_build: "npx vite build clean; git diff --check clean"
    electron_smoke: "29/29 — incl. mid-replay screenshot: Maple at the bench, pendulum rig drawing the real 96-sample arc, LAB REPLAY readout Δ0.11% (work/…/electron-grove-replay.png evidence)"
  evidence: "work/understory-grove-2026-09-20, /tmp/hemlock-gui-evidence/electron-grove-replay.png"
  claim_boundary: "Parse-failure and preview-session fixes target the exact errors seen in session-2026-09-20T07-42-14 receipts. Locomotion/replay verified in real Electron via screenshot + DOM. Maple still authors scaffold-fallback artifacts when it emits empty source — its model-side authoring quality is a live-lane question, not fixed by these host changes. No commit or push."
dream_gather_vjp_fix_2026_09_20:
  status: implemented_verified
  trigger: "Live Dream run cycle-2026-09-20T08-20-50-886Z failed at training iter 1: ValueError [gather_axis] Cannot calculate VJP with respect to indices — Maple's MoE routing indices were traced into the autodiff graph."
  root_cause: "config.json model_file=maple.py means the CHECKPOINT's bundled maple.py executes under --trust-remote-code, not the repo copy. Two traced-index gathers: (1) group_expert_select take_along_axis(scores, inds) where inds derives from argpartition over differentiable scores — the actual crash site; (2) MapleSwitchGLU passing idx to SwitchLinear gather_qmm/gather_mm rhs_indices — the guard upstream switch_layers.py has (if self.training: idx = mx.stop_gradient(idx)) that Maple's fork dropped."
  changes:
    checkpoint_maple_py: "Patched ~/Models/Hemlock/maple-2bit-mlx/maple.py — the executed copy: inds = mx.stop_gradient(inds) after argpartition in group_expert_select; if self.training: idx = mx.stop_gradient(idx) after _gather_sort in MapleSwitchGLU.__call__. Cleared stale __pycache__. Safetensors manifest only hashes *.safetensors, so base-weight integrity evidence is unaffected."
    repo_maple_py: "Same two fixes applied to mlx_lm/models/maple.py so the fork stays in sync — the repo copy is the source of truth for future checkpoints."
  verification:
    unit: "nn.value_and_grad through full MapleSparseMoeBlock (gate argpartition -> take_along_axis -> gather_sort -> SwitchLinear gathers) in training mode — both sorted (>=64 idx) and unsorted paths differentiate cleanly."
    live_rerun: "Re-ran the EXACT trainer command on the failed run's data dir (maple-2bit-mlx, lora rank-8 1-layer, adam 1e-5, batch 1, iters 4, max_seq 256): all 4 iters completed, train loss 2.658 -> 2.377, val ~0.826-0.828, adapter saved (0000004_adapters.safetensors 46.4MB + adapter_config.json), exit code 0. Evidence: workspace-runtime/cycle-fix-verify-2026-09-20T04-26-38/"
    base_integrity: "All 4 base safetensors byte-size match the failed run's base-weights-before.json manifest; training writes only to adapter-path."
  claim_boundary: "Dream LoRA training on Maple now runs end-to-end on the real checkpoint and real dataset — verified by live rerun, not just unit tests. Loss decreased and an adapter was produced; adapter quality/downstream inference improvement is unclaimed (that needs a served eval). An intermittent GIL finalization message appeared once at process teardown AFTER 100% + save (exit code remained 0 on the clean run); it's a benign shutdown race, worth watching but not blocking. The 306>256 token truncation warning persists — dataset-quality item, not a blocker. No commit or push."
rlcd_scored_choice_2026_09_20:
  status: implemented_verified
  trigger: "User pointed at notnotsamuel/LFM2.5-350M-RLCD — parallel constrained decoding (prefill once, batch-score candidates, assemble JSON in Python, 8-63x vs autoregressive). Applied the concept to Maple's agent action selection."
  changes:
    score_endpoint: "New POST /v1/score on the fork's mlx_lm server (server.py): takes messages|prompt + prompt_suffix + candidates[], prefills once with LRU prompt-cache reuse (fetched at prompt[:-1] so the last token is always forwarded), then teacher-forces each candidate against a deepcopied cache — one small forward per candidate. Returns per-candidate logprob + avgLogprob + tokens. Runs on the generation thread so it serializes with generation; non-batchable semantics (drains an active batch like other non-batchable requests). Old servers 404 -> scoreEndpointUnsupported flag degrades quietly for the session."
    scored_choice_path: "agent_orchestrator infer() now tries scored-choice before generation: buildScoreCandidates enumerates allowedNextCommands + terminal kinds as minimal candidate continuations — complete envelopes for input-free commands, one per experiment kind for experiment.run (6), one per profile for verify (3), decision prefixes for free-input commands (artifact.author, code.apply, file.search/read, context.search/query, receipt.inspect, experiment.note) and terminals. pickScoredCandidate picks max avgLogprob. A complete winner is already a valid compact envelope -> parseModelResult -> zero generated tokens, nothing to misparse. A prefix winner records progress.scoredDecision and falls through to generation for the free fields. Scoring failure -> degraded event + generative fallback."
    think_suffix: "Maple's chat template hardcodes <|im_start|>assistant\\n<think>\\n — scoring sends prompt_suffix '</think>\\n\\n' so candidates score as the action JSON directly (a no-think decision probe)."
  verification:
    unit: "4 new tests in agent_orchestrator.test.cjs: complete winner executes with zero generative calls + action.scored telemetry; prefix winner falls through with scoredDecision injected; scoring failure degrades quietly; candidate enumeration (6 experiment kinds, 3 verify profiles, all complete candidates parse as JSON) and pickScoredCandidate margin."
    endpoint_live: "Draft model (qwen25-0.5b) served the real endpoint: sane candidate ordering (answer beats tool calls for a Q&A prompt), prefix cache reuse confirmed (40 cached tokens on repeat)."
    maple_correctness: "VERIFIED ON REAL MAPLE: block-append teacher-forcing after chunked prefill matches a fresh full-forward within 0.18 logprob (bf16 noise). IMPORTANT INVERSION — the single-token decode path deviated 16 logprob because Model.__call__ routes 1-token calls through the approximate FlashHead (maple.py:986-993); the earlier 'block-append corrupts logits' belief was backwards. Scoring's multi-token path uses the exact lm_head."
    host_tests: "278/278 via npm run test:agent (+4)"
    renderer_tests: "13/13 groveBindings; full UI suite unchanged"
    production_build: "npx vite build clean; git diff --check clean"
  claim_boundary: "Scoring mechanism verified end-to-end on the draft model and numerically on real Maple weights. The live agent-loop win (latency vs autoregressive action envelopes, zero parse failures on complete winners) is unclaimed until a real Maple lane runs with a server carrying /v1/score — the running server needs a restart to pick up the new endpoint. Decision quality of no-think scoring vs think-then-generate is unmeasured; action.scored events carry margin telemetry for audit. Terminal/generative winners still pay a generative step. No commit or push."
autonomy_and_training_2026_09_20b:
  status: implemented_verified
  goal: "Agentic + autonomous but watchable/auditable/stoppable Maple; performance + training improvements."
  changes:
    real_pause_resume: "task.pause/task.resume commands wired to orchestrator.pauseTask + resumeTask. 'paused' now actually parks the loop: proposeNextAction returns early at the next step boundary (in-flight action finishes and records its observation), emits task.paused once; resumeTask flips status back to running and re-enters. Previously thread.pause set status=paused but the recursion kept stepping — pause was cosmetic."
    steer_into_actions: "task.steering now flows into compactTask for BOTH inferStructuredAction and scoreStructuredAction (last 4 entries, 200 chars each). Previously steering only reached the conversation lane — mid-task steer text never influenced action selection."
    queue_drain_fix: "AgentIntentQueue.drain now holds while the previous task is alive (waiting_for_approval, paused, running, waiting_for_user) — 'paused' added to ACTIVE_STATUSES. Previously drain started the next intent as soon as execute() returned, overwriting agentTask while the prior plan still awaited approval. New notifyTaskSettled() is called from the orchestrator emit wrapper on task.completed only — user cancel holds the queue (stopping shouldn't silently launch queued work)."
    campaign_autonomy: "Intent payloads with mode:'campaign' or autoApprove:true auto-approve their proposed plan inside processAgentIntent (approvePlan still clamps budgets), set task.autonomy='bounded-campaign', emit plan.auto_approved, and run to terminal. Renderer: 'campaign:'/'auto:'/'autonomous:' prefix in the composer submits a campaign intent. Queued campaign intents chain via the fixed drain = bounded multi-task autonomous runs, all receipted, pause/cancel live."
    metrics_surface: "HOST EXECUTION LOOP grid gained an INFERENCE cell (call count, total latency, repair count, campaign badge) — task.metrics was accumulated but never rendered."
    dream_examples_fix: "renderer startDream collected latestCodingExamples() for training.prepare but never sent them to startDream — coding examples were silently dropped from standalone Dreams. Now sent."
    dream_dataset_tokens: "dream_train.py write_dataset is now token-aware: loads the checkpoint tokenizer (char-estimate fallback), splits rows over the 448-token budget at assistant boundaries, trims unsplittable rows with recorded metadata (contentTrimmed/splitFromSource), reports tokenRange/rowsSplit/oversizedRowsRemaining in manifest.json. --max-seq-length raised 256->512. Fixes silent tail truncation AND the zero-supervised-token edge (--mask-prompt + prompt>=seq => ntoks=0)."
    adapter_grafting: "Successful standalone Dreams now auto-graft: server restarts WITH --adapter-path so the trained adapter is the default — previously the adapter lived only as renderer-side activeAdapterPath and every per-request adapters field paid a FULL multi-GB model reload (model_key includes adapter path). SIPS nested dreams pass autoGraft:false and keep their own compare-then-decide promotion. New grafts.jsonl registry under sipsDir + dream.adapter.grafted event; dream.detach command restarts bare (rollback); dream.grafts lists the registry. Dream Lab shows an Active graft card with a confirmable Detach button. Grove: dream.adapter.detached clears the graft halo instead of growing one."
  verification:
    host_tests: "282/282 npm run test:agent (+4: pause parks/resumes mid-plan, idempotent pause; queue drain holds on waiting_for_approval+paused, releases on terminal; paused counts active)"
    dataset_split: "Exercised write_dataset directly with stub + char-estimate tokenizers: 600-token row splits at assistant boundary, unsplittable oversize trims with recorded metadata, oversizedRowsRemaining=0"
    ui_tests: "115/118 (3 skips, 0 fail); vite build clean; git diff --check clean"
  claim_boundary: "Control semantics, queue semantics, dataset budgeting, and graft plumbing are unit-verified; the campaign auto-approve path is exercised through the queue tests but not yet in live Electron. Auto-graft correctness depends on startServer's --adapter-path handling (existing, previously dead-code path — needs one live Dream to confirm end-to-end). dream_train.py changes verified by direct function call, not a full live training run. No commit or push."
graduated_autonomy_2026_09_20:
  status: implemented_verified
  trigger: "User: too gated by approvals; wants a very autonomous but watchable/auditable/stoppable general agent."
  changes:
    autonomy_levels: "task.autonomy is now a real gate, not a label: bounded-local=supervised (unchanged), guided=plans auto-approve + sandboxed explicit commands (artifact/preview) are adaptively selectable and run without per-action clicks, autonomous/bounded-campaign=additionally permits every capability except train (always explicit). New UI: Supervised|Guided|Autonomous segmented control in the composer bar, persisted to hemlock-autonomy-v1, sent as payload.autonomy on every intent. Default is guided — the user's stated direction."
    adaptive_selection: "selectModelCommand + modelMaySelectCommand now take task autonomy — Maple can select artifact.author/preview.stop adaptively under guided+ (previously impossible: auto:false+explicit commands could never be adaptively selected, only run if pre-planned). Allowed-command list for scoring/generation widens identically, so /v1/score candidates cover them too."
    action_level: "The two waiting_for_approval action parks gain an autonomyAllows escape that emits action.autonomy.bypass for audit (defense-in-depth — host normalizes approval to plan|none so these mostly never fire)."
    pause_edge: "resumeTask restores the pre-pause status when no plan is approved yet (pause during waiting_for_approval then resume used to throw 'Approve a plan'); pauseTask stores pausedFrom and the park emit is idempotent."
  verification:
    host_tests: "284/284 (+2: autonomy opens guided artifact/preview but never train; guided task executes adaptively-selected explicit command while supervised never does)"
    electron_smoke: "35/35 real-Electron checks incl. intent parks at approval, task.pause/resume round-trip, campaign auto-approves to terminal with plan.auto_approved event, action.scored engages (degraded without server)"
    ui_tests: "115/118 (3 skips); vite build clean; git diff --check clean"
  claim_boundary: "Autonomy gating verified in real Electron end-to-end. Guided is now the default posture — supervised remains one click away. Train capability (dream/cycle/selfloop) always stays explicit regardless of level. Campaign intents are equivalent to autonomous per-intent. No commit or push."
agent_surface_overhaul_2026_09_20:
  status: implemented_verified
  trigger: "User: strong+broad subagent overhaul of agentic ability and harness flow; Hemlock must be very AI-friendly and let the AI inside improve the app itself."
  changes:
    input_contracts: "Every agentCommands descriptor gained inputHint (86 commands). allowedNextCommands propagates hint per entry; the action system prompt renders a fixed-priority INPUT CONTRACTS block (failure-prone commands first: artifact.author/update, code.apply, experiment.run/note, improve.propose, verify, file.*, context.*) — kept registry-static so the Maple prompt cache still holds."
    actionable_errors: "ACTIONABLE_HINTS map appends recovery guidance to failed command observations (artifact.author → supply complete input.source; preview.* → open first or pass sessionId; etc)."
    self_describe: "New auto command agent.capabilities returns the live registry (commandId/label/capability/auto/approval/inputHint) + task autonomy — Maple can enumerate its own toolset, zero budget cost."
    improve_lane: "improve.propose implemented (was a registered-but-handlerless command that always threw — the 'improve' intent could never finish). Bounded: {summary, rationale, files<=24 scoped via assertScopedPath, change<=8KB, confidence 0-1} → durable hemlock.agent.improve-proposal.v1 receipt in sipsDir/proposals/, deterministic content-hash id, receipt://proposed-improvement evidenceRef, never auto-applies. repo-map handler upgraded: fileCount/topDirs/keyFiles/workspaceDigest + repo://current-worktree evidenceRef, non-git fallback via rg --files."
    self_edit_verified: "Read lane already defaulted to repoRoot; default thread workspaceRoot IS the Hemlock repo; code.apply writes via assertScopedPath with approved-plan + digest guards; verify profiles (app-build/diff-check/python-tests) run against this repo. Full self-improvement loop: improve intent → repo-map → receipts.query → improve.propose → code.apply → verify → remember."
    renderer_qol: "ShortcutGuide 'Agent control' section (autonomy selector, steer:/campaign: prefixes, pause/resume, scored telemetry); autonomy badge on chat plan card + status bar; composer placeholder/hints advertise steer: while a task is active; queue readout with per-item cancel; conciseAgentNote covers paused/resumed/auto_approved/autonomy.bypass/scored/grafted/detached/steering.restarted; EventRow renders scored winner+margin compactly."
    docs: "AGENTS.md added at repo root — architecture, invariants, test commands, self-improvement loop. Readable by any agent including Maple via file.read."
  verification:
    host_tests: "284/284"
    ui_tests: "115/118 (3 skips); vite build clean; git diff --check clean"
    electron_smoke: "38/38 real-Electron checks incl. agent.capabilities returning improve.propose with inputHint, and improve.propose writing a scoped durable receipt"
  claim_boundary: "Prompt/contract/registry improvements verified by unit tests + build. Whether richer INPUT CONTRACTS measurably raise Maple's first-shot action validity needs a live lane run. Self-improvement apply path verified structurally (scoping + guards traced), not yet exercised end-to-end by Maple itself. No commit or push."
dream_fuse_2026_09_20:
  status: implemented
  trigger: "User: weights do not have to be immutable — the SHA manifest immutability was an arbitrary older-model safeguard."
  changes:
    dream_fuse: "New explicit train-capability command dream.fuse: merges the active graft (or payload.adapterPath) into a NEW checkpoint via python -m mlx_lm fuse --model <base> --adapter-path <adapters> --save-path sipsDir/fused/maple-fused-<stamp> --trust-remote-code, verifies the fused checkpoint, restarts the server on it. Base dir untouched → dream.detach restarts on base path = free rollback. grafts.jsonl records kind:fused entries; events dream.fuse.started/dream.fused."
    start_server: "startServer(adapterPath, modelOverride) — --model arg spliced on override; serverState.modelPath tracks the served checkpoint; detachGraft reports previousModel when reverting a fuse."
    renderer: "Dream Lab Active graft card gains 'Fuse into weights' (confirmable) beside Detach; narration for dream.fuse.started/dream.fused; grove marks fused grafts with kind:'fused'."
    helpers: "runPythonCommand(args,{timeoutMs}) — promisified spawnPython with stderr tail + 10m default timeout."
  verification:
    checks: "node --check clean; 284/284 host; 115/118 UI; vite build + diff clean. Fuse subprocess path NOT yet executed (needs a trained adapter + ~minutes of model load)."
  claim_boundary: "Policy change: weights are mutable; the before/after SHA manifest remains as provenance evidence (training still verifies isolation during the run), and fuse is an explicit separate act that writes a new checkpoint rather than mutating in place — rollback is detach. Whether mlx_lm fuse handles Maple's 2-bit quantized custom layers (MapleSwitchGLU experts) is untested — first real fuse run will show it."
continuous_kv_session_2026_09_20:
  status: implemented
  trigger: "User: Maple shouldn't warm up between every action/step/tool call — make chat/build continuous-state."
  changes:
    server: "mlx_lm/server.py — resolved model path now maps to launch adapter/draft defaults (omitted `adapters` inherits configured adapter instead of flipping model_key → kills a full multi-GB reload per step while grafted). /v1/score gains commit:true — teacher-forces argmax candidate + <|im_end|> into the prompt cache under prompt+cand+eos (BPE seam-safe: fused re-encode verified by prefix check, piecewise fallback). HEMLOCK_NO_PREFIX_CACHE=1 debug env; prompt_cache_bytes now actually reaches LRUPromptCache."
    orchestrator: "Per-task _sessionTurns replay prior steps verbatim as chat turns so each rendered prompt is a strict token-prefix extension of the committed key. Delta history (last entry only) once session exists; caps 40 turns/200k chars then rebase; cachedTokens===0 miss → clean rebase next step."
    main_cjs: "inferStructuredAction/scoreStructuredAction pass sessionTurns/sessionUserContent, return requestContent/cachedTokens for verbatim transcript recording."
  measured: "Real maple-2bit-mlx live: session mode step2 cached=1635/1770, step3 1784/1919; infer-after-commit ~22 new tokens/step. Old rebuilt mode: cached=0 every step (~1650-token full re-prefill). Determinism verified: identical greedy output cold vs cached."
  verification: "293/293 test:agent; node --check + py_compile clean; benchmarks/prefix_session_probe.py runnable against live server."
  claim_boundary: "Replay mismatches degrade to cache misses, never wrong output. Miss edge: reasoning-free generations replay with forced think opener. test_server.py TestNullPromptCache needs `requests` in venv to run in-suite (verified standalone). Not committed."
dream_lora_exit_fix_2026_09_20:
  status: fixed-verified
  trigger: "Runtime evidence: all 5 SIPS cycles on 2026-09-20 failed; two dream-runs completed all iters + saved adapters then died in interpreter finalization — no training-receipt.json written → counted as failures."
  root_cause: "python -m mlx_lm lora subprocess: worker threads (metal/tokenizer) call PyThreadState_Get after finalization begins → SIGABRT exit 134 even on success. dream_train.py saw nonzero exit → marked run failed."
  fix: "mlx_lm/lora.py main() — after run() returns cleanly: flush stdio + os._exit(0), skipping interpreter teardown (all work is done; adapters already saved)."
  verification: "Real maple-2bit-mlx + real cycle dataset (cycle-2026-09-20T08-20-50-886Z/data): before fix exit=134 after successful save; after fix 4/4 iters + 1/1 iter runs exit=0, adapters written."
  open: "The 08:20 gather_axis VJP crash did NOT reproduce on identical data/config (4/4 iters clean) — transient or already covered by existing stop_gradient guards in maple.py:451,720 and switch_layers.py:186,227. If it recurs, suspect a float-typed index reaching a gather under grad-checkpoint re-forward."
swarm_overhaul_2026_09_20:
  status: implemented
  trigger: "User: swarm of general + research agents to vastly improve grove, dreaming, chat/build interfaces; loosen arbitrary classifier-imposed restrictions; mine Application Support/Hemlock debugging data."
  agents:
    research_evidence: "Mined ~92MB model-output events + sessions + threads (Aug14–Sep20). Key findings: ~28% of structured-action calls end in unbounded reasoning with no content; 4096-token env ceiling truncated 6 records mid-envelope; repairFor.issues=[] echoed from prompt example; storage guard '10<10' display paradox; all 5 SIPS cycles failed (server readiness + lora exit crash); dream receipt loss via GIL finalization crash."
    grove: "groveScene.js/groveBindings.js/GroveSurface.jsx — resident life engine (seeded temperaments, waypoint strolls, spectator gather arcs on real experiment events), theatrical trajectory replays (ghost path, landmark motes, roaming glow, stage dim, watcher head-track, verdict reactions), ambience (aurora=dream heat, ash motes=failures, fireflies=bustle), focusOn(id), moon/mist/parallax. 27/27 own tests."
    dream_pipeline: "dream_train.py (quality gate, Jaccard near-dedup, 60% source-share balancing, rowHash provenance, deterministic holdout, resume via --resume-adapter-file, mid-run save-every, divergence early-stop + training-failed receipt, adapter-manifest.json versioning); sips_runtime.py (effective_statuses, fitness-ranked recall, memory-select action); memory_fitness.selectForTraining; tool_use_eval/live (taskMatch, compareRuns/compareLiveRuns, adapterPath fetch); verification_profile npm-run-test regex fix; readiness_probe tokenizer requirement; prompt_cache_stats aggregation. New test files ×3."
    chat_ui: "agentTimeline.js (75 host event types → transcript rows, task-scoped), HostEventRow expandable receipts, plan card live step states + progress, composer Pause/Resume/Steer, palette contextual actions, artifact viewport presets + diff stats + console badge, Command Center recent-work ledger + queue chip, FLIP window morphs + dock magnification (landed earlier in session)."
    degating: "agent_contracts/orchestrator/queue/kernel/preview/budget/context/main.cjs — ~50 caps raised to generous-but-bounded (steps 8→24, commands 12→40, wallClock 10→30min, stall watchdog 45→120s, file.read 20KB→64KB, session turns 40→80, inference repair 1→2 attempts, history/observation retention ~4×); inference fallback now executes next approved plan step instead of blocking (receipted via action.inference.fallback); repair maxAttempts bug fixed (literal 2 permanently overrode budget)."
    lora_exit: "mlx_lm/lora.py main() — flush + os._exit(0) after clean run() return; kills the GIL-finalization SIGABRT that marked successful Dream runs failed."
    integration: "dream.eval + memory.select commands registered+dispatched (memory.select ordered before memory.* wildcard); runDream payload forwards resume/resumeFrom/divergenceGuard/maxLossMultiple; assertDreamStorage shows 1-decimal GiB (no more '10<10' paradox); mapleMaxTokens floor 4096→8192; artifact-repair prompt example no longer teaches issues:[] (echo instruction added)."
  verification: "324/324 test:agent · 138/141 test:ui (3 skips) · vite build clean · git diff --check clean · lora subprocess verified exit 0 on real maple-2bit-mlx with real cycle dataset."
  claim_boundary: "Restriction audit agent's report still pending — its findings may surface caps the implementation agent missed. gather_axis VJP crash unreproduced (transient or guarded). dream.eval/live-comparison runs need a live server to exercise. Nothing committed."
restriction_audit_fixes_2026_09_20:
  status: implemented
  trigger: "Restriction-audit agent's ranked findings, applied after the de-gating tranche."
  changes:
    queue_drain: "main.cjs — queue now drains on task.cancelled AND task.blocked (not just completed); blocked/cancelled are terminal and previously stranded all pending intents forever. Parked states (approval/user/paused) still hold the slot. queue.drained_after_settle audit event fires when pending intents exist."
    anti_repeat: "agent_orchestrator.cjs — mutationIntervened(): a repeated commandId is fresh evidence when a mutating capability (write/artifact/train/memory) completed since its last run; fixes silent redirection of post-write re-inspection (repo-map/git.status after code.apply)."
    mutation_budget: "maxMutationSets 1→3; rollback + experiment.note exempt (a model that can't roll back its own apply was trapped); usage counts on SUCCESS only (was incremented before execution — a failed apply burned the slot)."
    adaptive_scope: "SAFE_ADAPTIVE_CAPABILITIES += task,memory — auto:true members (remember, memory.feedback/select, task.ask, thread.search, plan.propose, task.checkpoint) are now model-selectable; explicit lifecycle commands still gated."
    diagnostics: "summarizeOutput now carries issues[] + nested verification{status,issues[]} — model can see WHY verification failed instead of a bare status."
    caps: "objective 4000→12000 chars; steering context -4×200→-8×500; worldFindings 3→8 rows ×200 chars; maxTrainingCycles default honestly 1 (was 0 collapsing to 1 via || 1)."
    authoring: "Malformed artifact.author source now gets ONE bounded repair inference (reusing inferArtifactRepair's contract) before the canned 'night garden' scaffold — real content attempted first; repaired source tagged authoring.repaired_source, canned stays authoring.host_fallback."
  not_applied: "response_budget.cjs stays (used by e2e_artistic_fixture — not dead, just unused in the live path); thread.search scanLimit and readConversation window left to the thread-management rework; candidate-cap ordering left as-is (caps already raised 48→96/32→64); canned template remains as last-resort fallback (kept deliberately — honest evidence tag, preview-verify still runs on it)."
  verification: "324/324 test:agent green with all policy changes."
decide_endpoint_2026_09_20:
  status: implemented_verified_live
  trigger: "Kev-style decision contract adopted on Maple (jaredpalmer/kev evaluated: architecture yes, torch sidecar no — Maple is the judge)."
  server: "mlx_lm/server.py — POST /v1/decide: {state, questions{noul|choice|score}}; state prefilled once on prompt cache, each question forks the state cache (isolation by fork, suits untrimmable RotatingKVCache), option labels teacher-forced from a <decide> marker; softmax probabilities + confidence; commit writes prompt+suffix+winner.continuation+eot. _serve_score refactored into shared helpers (_seam_extend/_prefill_tokens/_score_continuations/_commit_continuation); score behavior unchanged."
  commit_fix: "_commit_continuation now rebuilds the cache under the fused re-encode when BPE merges cross the prompt/continuation boundary — previously stored prompt+encode(cont)+eot, a key the replay could never hit. Applies to score and decide."
  harness: "main.cjs decideStructuredAction + postDecideRequest (404→permanent fallback, HEMLOCK_NO_DECIDE=1 disables); orchestrator prefers decide over score, emits probability/confidence/margin/indecisive on action.scored, confidence<0.35 falls through to generative fill-in; memory.select reranks top-12 by expected usefulness score. pickDecidedCandidate/expectedScoreLevel/rerankByExpectedScore in agent_contracts.cjs."
  verification: "tests/test_server.py TestDecide 7/7 under runtime python; node --test electron 334/334; live Maple smoke: all three types answered, repeat call 27/27 cached tokens, 31s→2.2s."
  checkpoint_fix: "task.blocked checkpoints now carry blockedReason (payload reason/error) instead of only the event type — runtime debugging data gets a legible cause."
megaplan_decide_and_ui_overhaul_2026_09_20:
  status: implemented
  swarm: "7 agents across 2 waves + integration (1 extraction agent died mid-run, restarted clean)."
  wave0: "main.jsx 3148→~2400 lines; render functions extracted to src/windows/{ChatWindow,ArtifactStudio,CommandCenter,UtilityWindows}.jsx via 184-key ctx bag; shared helpers in windows/shared.jsx; per-window css stubs; behavior-identical (138/141 UI tests unchanged)."
  decide: "POST /v1/decide verified live on Maple — noul/choice/score calibrated probabilities, question isolation by cache fork, repeat calls hit prompt cache (31s→2.2s). Orchestrator prefers decide, falls back to /v1/score, confidence<0.35 defers to generation. memory.select reranks by expected usefulness. See decide_endpoint_2026_09_20."
  chat_window: "transcript search (kind chips + mark highlighting), message actions (edit-resend, raw view), per-thread draft persistence, prefix-mode hint chips + slash-command interception (unknown /cmd → notice, never an objective), scroll pin + jump-to-latest + unread divider, blocked-task recovery row (edit-resubmit/retry/receipts), friendlyError mapping + copy-details, transcript 160px floor with rail auto-collapse, context-stale chip, host-fallback honesty copy + chips. Modules: transcriptSearch, chatCommands, friendlyError, draftStore, contextStatus."
  artifact_studio: "multi-file source tabs + dep-free syntax highlighting, real LCS per-file diff, interact palette for all 13 REGISTERED_ACTIONS (click/type/key/scroll/hover/focus/wait/screenshot/resize/pause/stop), send-console-errors-to-Maple draft, host_fallback placeholder banner + repaired_source note, pending-approval chip linking to Chat, durable notice stack, title envelope-stripping, layout/view/viewport persistence, revision status dots. Modules: sourceTabs, syntaxTokens, artifactDiff, artifactMeta, noticeStack, studioPrefs, previewPalette."
  shell_nav: "fuzzy palette ranking + recents (commandPalette.js), dock activity badges (amber pulsing approval chip on chat — the #1 missed affordance in runtime data), ⌘` window cycling + ⌘W close + focus handoff, WindowBoundary error containment per window, palette combobox a11y, revise-title clean field, render-loop resilience."
  integration: "ctx gains resetConversationContext (/clear wired) + setArtifactLayout; readArtifactLayout merges hemlock.artifact.layout; payload.title feeds thread auto-title; blocked checkpoints carry blockedReason."
  verification: "334/334 host tests · 231/234 UI (3 skip) · build clean · diff --check clean · live Chrome smoke: slash interception, artifact panes, dock badges render, zero console errors."
  deferred: "⌘W on macOS needs a custom Menu template (Electron menu accelerators pre-empt before-input-event) — ⌘⌥W works everywhere; preview.pause has no host command (routes to preview.stop); in-page screenshot handler would make screenshotRef live."
perf_capability_megaplan_2026_09_20:
  status: implemented_verified
  trigger: "User: megaplan — improve performance and capability across the board. Decisions: bounded shell.exec, batch {actions:[...]} envelopes, artifacts plan-gated, balanced swarm."
  evidence: "Agent mined ~12 sessions / 2k events: /v1/score dominates (median 36s, p90 ~69s, max 117s, up to 69 candidates); structured action gen bimodal (cached continuation ~9s vs free-form ~52s); cold KV cache = 3-12x first-inference penalty; orphaned young 'running' snapshots strand the queue after restart; budget-exhausted experiment buttons accepted click storms (25 blocked events)."
  server:
    persistent_cache: "mlx_lm/server.py --prompt-cache-file: MRU entry saved on every-32nd insert + on shutdown via a save sentinel pushed into the request queue (save must run on the generation thread — its KV arrays' Metal stream is thread-local; 'no Stream(gpu,0)' otherwise). Warm load materializes lazy safetensors arrays on the loading thread via mx.eval (generation thread has no cpu stream — 'no Stream(cpu,1)' crash otherwise). Shutdown: no join — os._exit(0) after save, avoids PyThreadState_Get finalization crash (lora.py class). Verified live on 8081: save on SIGTERM -> warm load '24 tokens' -> repeat request cached_tokens 15/16 in 0.83s -> clean re-save + port free."
    assistant_prefix: "CompletionRequest.assistant_prefix (<=64 tokens): pre-baked '{\"command\":' into structured-action generation; included in cache keys; rejected on raw/score/decide paths with 400. main.cjs forwards it and retries once without on 400 (inference.fallback degraded event)."
  harness:
    shell_exec: "electron/shell_exec.cjs — bounded exec: bare executable names only, no paths/shells/wrappers (sudo/sh/env/npx/ssh/curl rejected), workspace-scoped path args, git limited to read-only subcommands, 60s timeout, 32KB output cap, hemlock.agent.exec.v1 receipts. 14/14 own tests."
    batch_envelopes: "{actions:[...]} multi-action envelopes validated per-action, executed sequentially, budgets/receipts per step."
    artifact_plan_gate: "artifact.author + artifact.update approval:'plan' — plan approval is the user boundary; code.apply (repo writes) was already plan-gated."
    decide_fastpath: "decide skipped when only 1 candidate; top-k candidate pruning before /v1/score (was scoring up to 69 candidates at ~36s median); verify profiles +lint/typecheck/npm-test with honest skip on missing prereqs."
  renderer:
    ctx_slices: "src/ctxSlices.js — per-window key manifests + memoized pickCtx; windows get slices not the 184-key bag (was re-rendering every window per host event)."
    event_buffer: "src/eventBuffer.js — ~16ms coalescing, cap-based flush, unmount-safe."
    transcript_window: "150-message render window + Show-earlier; search spans full transcript and expands window to matches."
    budget_latch: "experiment buttons disable on budget exhaustion; latch resets on task lifecycle events."
  session_fix: "task snapshots carry sessionId; thread.switch blocks any snapshot written by a different session regardless of age (was >10min only — a 5-min-old orphan stayed 'running' and stranded the queue). Legacy snapshots keep the age fallback."
  verification: "361/361 host · 242/245 UI (3 skip) · 45/45 pytest · py_compile clean · vite build clean · live 8081 round-trip proven end-to-end."
  claim_boundary: "Port-8080 app server runs older code until next app restart — cache persistence activates then. os._exit(0) shutdown skips interpreter cleanup by design (adapters/files already flushed). Batch envelopes and shell.exec receipts verified in tests; live Maple exercise pending a real task. Nothing committed."
stability_agentic_persistence_megaplan_2026_09_21:
  status: implemented
  trigger: "User: greatly improve stability, agentic behavior, persistence, dreaming, GUI (UX + AX). Swarm of 4 agents + integration. Decisions: dream idle-proposal->candidate; decide executes plan + logs divergence; bounded auto-resume on crash; world-state + grove writes for AX."
  evidence_bugs_fixed:
    dream_budget: "dream:start inherited the resumed foreground task's maxTrainingCycles:0 -> TRAINING_BUDGET_EXHAUSTED blocked 'Run local Dream' 3x. startCommandOperation now mints a run-scoped kernel task with fresh budget for user-initiated train commands; agent-initiated runs still spend the task's own budget. Also: a new intent on a stale task now mints a new task (task.retargeted) instead of overwriting the old objective."
    decide_divergence: "action.scored winners (e.g. {kind:blocked} @0.99) were ignored while the fill-in prompt said 'continue the scored command choice' -> model parroted 'Blocked...' and once literally emitted {kind:blocked}, killing a task in 1 step. Now: action.scored.divergence event when executed != decided; prompt names the EXECUTED action and forbids restating the scored choice."
    compact_args: "Compact-choice envelopes (commandId + input:{}) failed deterministic-input -> instant block. Now: plan-step input merges into empty action input; deterministic-input failures get ONE bounded input-repair inference (required-arg schema echoed; commandId locked); second failure blocks honestly."
    metrics: "commandsUsed stayed 0 (syncTask stomped kernel increments — also defeated the budget ceiling); wallClockStartedAt only set lazily; inferenceReady stomped false by waitForServer throwing on ADOPTED servers (serverProcess==null) — serverState.adopted marker fixes the root cause."
  stability:
    health_monitor: "NEW health_monitor.cjs — polls /health only while a task is active; maple.health.failed once per outage at threshold, .recovered on heal; outage feeds the crash budget + bounded respawn. Idle sessions never poll."
    crash_capture: "uncaughtException/unhandledRejection -> durable runtime.error event then existing fatal semantics; webContents render-process-gone -> renderer.crashed + bounded auto-reload (1/60s/window); unresponsive -> event only."
    auto_resume: "Unexpected server exit mid-task -> pending flag; after respawn warmup settles, resumeTask once from the last durable action (prompt-cache-file warms the prefill). Second exit under same task -> blockTask('server restarted twice'). Never double-drives a live loop."
    crash_persist: "mapleCrashTimestamps persist via sessions/<id>/maple-runtime.json + sanitizeCrashHistory load-time prune — crash-loop memory survives app restart."
  agentic:
    plan_revise: "NEW plan.revise (approval:'plan'): model proposes replace/insert/remove of the remaining suffix of an approved plan; executed prefix immutable; every step validated against registry + allowedNextCommands; whole-revision reject leaves plan untouched; plan.revised carries a {kept,removed,added} diff."
    steering: "compactTask.steering now filters to pending items in all three action transports; marked delivered post-response (task.steering.delivered); mid-inference steer aborts and stays pending for the next step."
    fence: "extractJsonObject deterministically strips a trailing ``` fence before parse."
  persistence:
    queue: "agent_queue persistablePending/restorePending — pending intents serialize into state.json and restore as 'queued' (deduped by objective, never auto-executed, individually cancellable); queue.restored event on boot."
    events: "session events.jsonl append-only (pre-existing); renderer.error + runtime.error now also land in the spine."
  dreaming:
    idle_scheduler: "maybeProposeIdleDream — after queue settle + boot, when queue empty/server idle/no dream or sips running and >=8 new dataset rows since last dream -> dream.proposal.created -> ambient-inbox candidate ('Dream cycle ready — N new findings'). Accept -> normal explicit train approval (invariant preserved). 30min cooldown + dismissal-aware; HEMLOCK_NO_IDLE_DREAM=1 disables."
    receipts: "dream.completed carries receiptSummary (iters, first/final/best loss, lossTrend, dataset rows, adapterPath, evalVerdict); dream.eval hardened — renamed receipts so receipts.query surfaces them, zero-response lanes -> dream.eval.degraded, thrown failures -> failed receipt + degraded result (never a crash/false verdict)."
  ax_gui:
    agent_self: "NEW agent.self (auto, read): task/phase/budget, queue depth, pendingApprovals, server readiness, counts (artifacts/memories/findings), last 5 receipts — the agent's self-model."
    world_state: "NEW world.state (auto, read): grove projection — experiment landmarks, recent receipts, findings count, placed markers."
    world_place: "NEW world.place (approval:'plan', write): Maple places persistent grove markers/monuments/signs — durable JSONL store, world.placed event, seeded placement; rendered as glowing labeled waypoints in the grove (markers rail in GroveSurface)."
    renderer_errors: "WindowBoundary -> hemlock:renderer-error IPC -> durable renderer.error event (bounded payload, 60s same-message dedupe)."
    gui: "Dream window gains cycle history/dataset stats/eval verdict sections; dream.cycle candidates get a distinct .is-dream glyph in the ambient inbox; palette gains world.state + maple.launch actions."
  verification: "392/392 host · 249/252 UI (3 skip) · vite build clean · git diff --check clean. No live-app smoke (app not running); IPC paths covered by host_ux_ipc tests."
  known_quirks: "state.json is a bare task snapshot but the boot restore reads .task — the whole prior-task restore path is dead code (both agents confirmed; deliberately left inert — thread.switch sessionId restore covers the needed path). world.place under supervised mode requires being a planned step (approval:'plan' carve-out admits it on approved plans). Monitor-driven restarts count against the crash budget."
swarm_polish_2026_09_21:
  status: implemented
  trigger: "User: continue with large swarm improvements, polishing, updates. 5 background agents (E2E/HOST/MEM/WORLD/UI) + parent integration."
  host_lifecycle:
    boot_restore: "Dead path revived: state.json reader accepts bare hemlock.agent.task.v1 snapshots AND envelope {task} shape; non-terminal tasks boot as blocked with the canonical guard reason, emit task.restored, never auto-run. Restored/default registry threads parked on boot (thread.parked_on_boot) so none mint stale 'running' rows."
    preview_reap: "Boot diffs artifact.preview.ready vs .stopped in the previous session journal; orphans emit preview.sessions.reaped."
    response_budget: "Audited — dead in the live path (only e2e_artistic_fixture.cjs imports it); live path uses mapleMaxTokens. Header comment marks the sole consumer."
    artifact_registry: "writeJson now tmp+fsync+rename (was non-atomic — mid-write crash truncated the manifest); update() allocates max(revision,...revisions[].revision)+1 — post-restore updates no longer collide with on-disk revision dirs."
    reachability: "Audited — shell.exec/world.place/experiment.suggest all reachable via modelMaySelectCommand + approval:'plan' carve-out. Zero shell.exec usage was proposal-side: defaultPlanSteps never named it."
  memory:
    consolidate: "NEW memory.consolidate (approval:'plan') — Jaccard clusters at 0.7 threshold (dream_train's 0.85 misses title-stem dupes); append-only: consolidate overlay + demote transition per absorbed record, mergedBody inherits novel sentences, auditId enables memory.rollback undo. dryRun previews."
    list_rank: "NEW memory.list (auto, read) — effectiveStatus, feedback, rankScore (fitness + recencyBonus ≤10 keyed on freshest of promotedAt/lastUsedAt/createdAt), ageDays, sourceRefs, nearDuplicateOf. recall + memory.select now sort by rankScore — stale lessons stop outranking fresh confirmed ones."
    auto_demote_fix: "memory_feedback returned the record's raw status field — transition-promoted records keep status:'candidate' forever, so the >useful auto-demote gate could never fire. Now returns replayed effective status."
  world:
    suggest: "NEW experiment.suggest (auto, read) — ranks never-run kinds, open hypotheses, unexplored input regions from durable receipts + dataset only. clampInputs hardened: unknown keys/non-finite values throw INVALID_EXPERIMENT_INPUT naming valid params."
    hypothesis: "experiment.run accepts {hypothesis}; a finding citing a hypothesized run flips its receipt hypothesisOutcome open->addressed (host never claims confirmed/refuted)."
    schema_fix: "REAL BUG: runWorldExperiment spread ...result AFTER the schema field — every on-disk receipt carried hemlock.experiment.result.v1 and failed loadExperimentReceipt's schema check; only the in-memory map masked it. Envelope fields now come after the spread."
  e2e:
    suite: "NEW e2e_autonomy_surface.test.cjs (12 tests) — real main.cjs under electron-mock + spawn-guard (only EXEC_ALLOWLIST bare names reach real spawn); dead-port apiBase pins inference failure so the deterministic plan-step fallback path runs. Covers: batch ordering+receipts, real shell.exec + pre-spawn denials, world.place gate + persistence, agent.self shape, plan.revise suffix rewrite + task completion, one-shot input repair, decide divergence durability, queue persist/restore + idle-dream proposal."
    stream_leak_fix: "REAL BUG fixed by parent: refused structured-action fetches threw between startStream and any finishStream — agent_action rows leaked into activeStreams forever, permanently gating maybeProposeIdleDream's !activeStreams.size check while Maple was down. sendActionRequest + SSE reader.read now finishStream(failed) on throw."
  ui:
    toasts: "workToasts.js — quiet bottom-right toasts on terminal work events (task.*, dream.*, memory.consolidated, world.suggest, crash-loop), id-dedupe, 12s TTL, cap 3, click routes to the owning window."
    memory_window: "Memory window renders real memory.list inventory — effectiveStatus labels, ageDays staleness badge (>=90d), cluster badges, Source->Receipts deep link, plan-gated Consolidate + Refresh."
    receipts: "Receipt rows expandable: bounded prettyPayload, evidence ref list with Copy ref, Open-in-<window> deep links via receiptTargetWindow mapping."
    shortcuts: "ShortcutGuide bidirectional parity test — every literal binding in handlers must be documented, every documented chord must have a backing handler."
    lane_labels: "ChatWindow hardcoded 'Maple' labels -> ctx.selectedLane (Codex/Claude lanes render honestly)."
  plan_defaults: "defaultPlanSteps: experiment intent leads with experiment.suggest; memory intent leads with memory.list; coding intent adds shell.exec when the objective mentions test/run/build/lint — the new surface is now reachable from canned plans, not just adaptive selection."
  verification: "440/440 host · 259/262 UI (3 skip) · 45/45 test_server.py · py_compile clean · vite build clean · git diff --check clean."
  known_gaps: "pytest env: test_evaluate.py needs lm_eval (not installed), test_datsets::test_hf fails on an hf-hub URI parse — both pre-existing env gaps. Live-Maple exercise of the full surface still pending a real task run. Nothing committed."
ux_ax_os_swarm_2026_09_21:
  status: implemented
  trigger: "User: mega swarm on UX/AX for a high-quality OS + model versatility + less warm-up while steerable + in-app thread management. 5 agents + parent. Decisions: full Threads window; BOTH warm levers; BOTH versatility (lane fallback + vocabulary); agent-led AX with human polish."
  threads:
    window: "NEW src/windows/ThreadsWindow.jsx — dedicated management surface: open+archived lists, host-side thread.search, conversation preview (40 msgs), checkpoint list with per-checkpoint restore, lifecycle actions (switch/rename/pause/resume/archive/restore/cancel/delete), fork with forkedFrom provenance chip + 'switch to fork'."
    commands: "NEW thread.checkpoints (auto read), thread.conversation (auto read), thread.fork (explicit, reversible — mints thread with provenance, does not switch), thread.checkpoint.restore (explicit), thread.delete (explicit, NOT reversible — deletes history files). forkThread added to thread_manager.cjs; normalizeThread carries forkedFrom."
    wiring: "WINDOW_META.threads, ctxSlices.threads manifest, windowManager def, APP_SHORTCUTS slot (no digit chord — past 9), palette entries."
  warm_path:
    multi_entry_cache: "Prompt cache persistence is now top-K (HEMLOCK_PROMPT_CACHE_SLOTS, default 4, <=1 reverts to legacy). LRUPromptCache.recent_entries(n) enumerates hottest held entries; {path}.manifest.json (atomic commit point) + {path}.<i>.safetensors per entry; load is oldest->MRU so recency is preserved; per-entry model_key/key_tokens validation skips bad entries; manifest-corrupt falls back to legacy single file; all-bad -> cold start. LIVE VERIFIED: SIGTERM -> manifest+2 entries -> restart 'Loaded 2 prompt cache entries' -> repeat request 14/15 cached_tokens."
    warm_prefill: "NEW maple.warm command + automatic warm prefill on server-ready transition and thread.switched (when server ready): rebuilds the exact structured-action system prefix via shared buildActionSystemPrompt (extracted module-level in orchestrator so warm bytes can't drift), POSTs max_tokens:1. Skips while any stream/task/dream/sips is active or inside 60s cooldown (HEMLOCK_WARM_COOLDOWN_MS); emits maple.warm.prefill (passed/degraded/skipped); serverState.lastWarmAt/warmCachedTokens exposed; never throws into boot/switch."
  versatility:
    lane_fallback: "When structured-action inference dies of transport-death/gpu-server-error/stall (NOT parse/envelope failures), the orchestrator retries the step once via the configured fallback lane: task.fallbackLane > HEMLOCK_FALLBACK_LANE > none. Emits action.lane.fallback BEFORE the attempt; action+observation stamped provider/fallbackFrom:'maple' so receipts never claim Maple did the work; fallback failure blocks LANE_FALLBACK_EXHAUSTED naming both errors; unconfigured = identical old behavior; capacity lease + provider auth still enforced on the fallback lane."
    memory_note: "NEW memory.note (capability memory, approval plan, countsAgainstBudget) — Maple can now write lessons: runSipsRuntime record -> status candidate, <=4000-char clamp, evidenceRefs[0]->evidencePath, memory.noted event. Promotion still via memory.promote (append-only preserved)."
  ax_depth:
    failure_hints: "contracts.FAILURE_HINTS — host-authored <=240-char next-step hints for every failure class (deterministic-input names required fields via inputHint, budget-exhausted names the budget, plan-gate names the unblock path, transport-death says don't blind-retry, etc). failureHint()/resolveFailureClass() pure exports; createObservation+compactObservation carry commandId/errorClass/suggestion; kernel stamps them on action.faild journal + observation.recorded."
    obs_bug: "REAL BUG: {status:'failed'} results collapsed to 'passed' in createObservation — every thrown-command failure observation was recorded as passed. Fixed."
    self_capabilities: "agent.self gains recentFailures (last 5 with class+suggestion), allowedNow, memory summary {records/promoted/candidates/demoted/lastConsolidatedAt}, server.lastWarmAt/warmCachedTokens (defensive). agent.capabilities gains selectable/byCapability groups with inputHints + freeReads (countsAgainstBudget:false)."
  ux:
    notifications: "src/notificationHistory.js — derives the last ~30 terminal work events from the event spine via workToastFor rules; CommandCenter NOTIFICATIONS rail (6 rows) + Activity full history; click routes to evidence window."
    status_chips: "src/statusChips.js — runtimeChip (cold/warming/ready/down honest about process-vs-inference), activeJobChip, queueChip, liveRateChip, warmCacheChip (defensive reads of lastWarmAt/warmCachedTokens); HEARTBEAT panel chip line."
    polish: "Empty-state upgrades across all windows (each says purpose + the one action that fills it); grove marker rail scroll-clamped with slow-load honesty text; titlebar tooltip exposes drag + double-click fill/restore."
  verification: "471/471 host · 278/281 UI (3 skip) · 50/50 test_server.py · py_compile clean · vite build clean · git diff --check clean · live 8081 multi-entry round-trip proven (2 entries saved/loaded, 14/15 cached tokens)."
  known_gaps: "Warm prefill warms the system prefix only (volatile plan/action tail deliberately not warmed — documented); lane fallback requires HEMLOCK_FALLBACK_LANE or task.fallbackLane configured (default none); pytest env still lacks lm_eval + hf-hub dataset parse fails (pre-existing). Nothing committed."

maple-speed-dream-settings-2026-09-22:
  date: "2026-09-22"
  rev: 8a5b0c8
  scope: "swarm tranche: Maple speed/context (measured), Dream agentic-SFT + preview, context budget, Settings fill-out"
  speed:
    bench: "NEW dream-chat/scripts/maple_bench.py — live-server probe: decode (tok/s+TTFT), prefill (t/s+cached_tokens), score (per-candidate ms); --context-tokens N for long-context decode; stdlib only. Baseline/after receipts in .optimize/runs/."
    measured: "LIVE VERIFIED on maple-2bit-mlx :8081: decode 30.65 -> ~64 tok/s (+2x), TTFT 393 -> ~140ms, prefill 182.6 -> ~419 t/s (3165-tok prompt), /v1/score 318 -> ~125ms/candidate. kv-bits=8 vs exact at 2455-tok context: 72.8 vs 57.1 tok/s (+27%). Identical greedy completion between exact and quantized configs."
    decode_fix: "maple.py: single shared (pos,eps) mx.array per decode step — float(cache.offset) per layer forced a GPU sync ~24x per token; layers now receive the step's shared array. Fused qk norm+rope kernel retained."
    kv_quant: "--kv-bits/--quantized-kv-start (server) + HEMLOCK_KV_BITS/HEMLOCK_KV_QUANT_START (host serverArgs, default 0/off + 5000). generate.py maybe_quantize_kv_cache now only converts true KVCache — CRASH FIX: RotatingKVCache.to_quantized raised NotImplementedError which aborted the Metal command buffer (observed server kill); sliding-window layers stay exact, 6 full-attn layers quantize past the start token."
    lora_fix: "lora.py ends with os._exit(0) after run() — Metal compile workers called back into a finalizing interpreter (PyThreadState_Get SIGABRT) making completed Dream trainings exit 134."
  dream:
    agentic_sft: "NEW agent-spine dataset source: collectAgenticSpine (main.cjs) gathers durable projection.jsonl + sessions events.jsonl; dream_train.py rebuilds each step's request at its own timestamp (faithful Python ports of allowedNextCommands/completedTaskCommands/observation summarizer) and emits chat rows ONLY for completed actions with passed/observed observations — failed/blocked/malformed actions never train. system = real buildActionSystemPrompt bytes; assistant = executed envelope. Dedup on (kind,commandId,input), share-capped, structural request compaction for token budget, manifest gains agenticSpine stats."
    preview: "NEW dream.dataset.preview (read, auto, free) — runs dataset composition without training; returns source counts, digest, token stats, bounded samples."
    fixes: "resolve_training_config no longer crashes on non-numeric iters/numLayers/maxLossMultiple; metrics_summary surfaces lossTrend + finalValPerplexity into receipts."
  context:
    accounting: "task.contextUsage from REAL server usage (prompt/completion/cached tokens) across all 6 inference call sites; metrics.promptTokens/cachedTokens/contextPeakTokens cumulative; agent.self gains context {usedTokens,cachedTokens,estimatedTokens,maxTokens,modelMaxTokens} — estimates labeled estimatedTokens, never fabricated."
    guard: "contextMaxTokens budget (default 24000, HEMLOCK_CONTEXT_MAX_TOKENS, min 4096): chars/4 estimate projects the volatile request region pre-call; over-budget -> compactActionContext (slim stale detail fields -> drop replayed transcript front -> shrink history tail -> drop transcript+re-expand) -> still over -> CONTEXT_BUDGET_EXHAUSTED honest block naming fresh-thread/reset suggestion. Every pass receipts context.compacted; progress.contextCompaction.note tells Maple what dropped. Static system block untouched (cache holds)."
    conversation_trim: "NEW conversation.trim (explicit) -> threadManager.trimConversation: keeps newest messages, archives dropped prefix to <log>.<ts>-trim-archive.jsonl."
  settings:
    window: "SettingsWindow gains Dependencies (deps.check rows + recheck), Runtime tuning, Agent defaults, Danger zone (prompt-cache path+size, confirmDialog-gated clear) — existing SettingsWorkspace untouched."
    commands: "NEW deps.check (auto, free — python3/mlx/mlx_lm/transformers/maple-model/node via the SAME pythonEnvironment the server launches under; no torch probe — nothing in this stack uses torch), settings.get/set (persisted <dataDir>/settings.json, per-field appliesOn: live vs next-launch), settings.clearPromptCache (explicit)."
    knobs: "promptCacheSlots, prefillStepSize, kvBits (0/8/4), fallbackLane (live), warmCooldownMs, autonomyDefault+reasoningLevel (live). applyRuntimeSettingsEnv() pushes stored env-backed values at boot before serverArgs freeze; prefillStepSize patches the mutable flag in place."
  verification: "485/485 host (context_budget 10, dream_agentic 4, maple_warm etc.) · 288/291 UI (3 skip) · 50/50 test_server.py · py_compile + node --check clean · vite build clean · git diff --check clean · live benches above."
  housekeeping: "Removed npm global pytorch@0.0.1 stub (namespace placeholder, not real PyTorch — user installed it chasing a 'pytorch not found' message; no torch dependency exists in this stack)."
  known_gaps: "kv-bits decode gain measured at 2.4k context (confound: warm-vs-cold prefill paths differ); n-gram speculative still incompatible with SWA-512; pytest env lacks lm_eval + hf-hub billsum parse fails (pre-existing); SPEED agent's own run died with the host machine mid-pass — parent completed its after-benchmarks + settings knob. Nothing committed."

ux-polish-ctx64-quality-perf2-2026-09-22:
  date: "2026-09-22"
  rev: 8a5b0c8+dirty
  scope: "swarm tranche: deep UX polish + 64k context + action quality + measured perf-2"
  context_64k:
    ceiling: "resolveContextMaxTokens: stored pin > HEMLOCK_CONTEXT_MAX_TOKENS > kv-scaled default (24000 exact / 61440 kv-bits) > clamp to modelMax-2048 (125952). DEFAULT_BUDGET.contextMaxTokens=null makes unpinned tasks re-resolve env per request — live updates actually apply. Clamp never silent: warn-once + durable context.ceiling.clamped + contextUsage.requestedMaxTokens/maxTokensClamped/kvBits."
    cache_bytes: "defaultMaplePromptCacheBytes(kvBits): exact 768M (one 64k fp16 entry ~768MB) / quantized 1G (64k 8-bit entry ~384MB + ~18MB sliding). Math comment in main.cjs."
    settings: "contextMaxTokens knob (4096-124000, appliesOn live — genuinely live for unpinned tasks); kvBits def existed but was DEAD (env write couldn't reach frozen serverArgs) — applyRuntimeSettingEnv now patches --kv-bits/--quantized-kv-start/--prompt-cache-bytes in-place like prefillStepSize. settingsModel.js mirror updated."
    self: "agent.self.context gains maxTokens(requested)/practicalCeiling(real bound)/maxTokensClamped/kvBits; modelContextCeiling publishes HEMLOCK_MODEL_MAX_TOKENS for the budget seam."
  perf2:
    checkpoint_sync: "REAL FIND: the served model loads ~/Models/Hemlock/maple-2bit-mlx/maple.py via trust_remote_code — it PREDATED the prior speed pass, silently losing ~20 tok/s. Synced repo->checkpoint (backup .bak-perf2); decode 72 -> 88.5-90.8 tok/s. Regenerating the checkpoint MUST ship the repo maple.py."
    profile: "Step is ~1005 nodes/~470 kernels, eval 11.6ms — MoE gather_qmm pair dominates at 7.25ms; attention 2.55ms. simdgroup-matrix expert kernels are the next real lever (custom fused-expert attempt: correct but 0.7ms vs 0.5ms eager — reverted); mx.compile over expert block diverges on router atomic counter (reverted)."
    kept: "maple.py _add_rms_norm drops input reshapes + flat fused qkv row (fewer nodes/layer, measured neutral, zero-risk); generate.py GenerationBatch._step B==1 sampler fast path. Probes: .optimize/prof_gpu.py, prof_step.py, probe_compile.py, probe_moe.py."
  quality:
    coverage: "Full 126-command reachability map in report: memory.note/world.state/dream.dataset.preview/agent.capabilities wired into defaultPlanSteps; COMMAND_EVIDENCE + actionInputContract extended (improve.propose/memory.note/world.place/shell.exec/plan.revise/task.ask); SCORE_GENERATIVE_COMMANDS +15 so required-input commands can't win as zero-token {} envelopes (the production improve.propose block class)."
    decide: "DECIDE_MIN_MARGIN=0.1 top1-top2 gate added to the 0.35 confidence gate — near-ties fall through to generation; margin/indecisive receipted for recalibration."
    channel_rejoin: "REAL PRODUCTION FIX: forced-prefix JSON split across content/reasoning channels caused PARSE_FAILs (3 observed sessions). asModelResult carries reasoning; parseModelResult rejoins once through full validate, parseStatus channel-rejoined + receipt. Static prompt gains one line: emit whole JSON in content channel."
    classify: "classifyFailure catch-all resolves via resolveFailureClass(code,{message}) — budget/malformed/approval failures no longer burn the one input-repair retry (test: COMMAND_BUDGET_EXHAUSTED blocks with zero repair calls)."
  ux:
    nav: "windowCycleOrder/nextWindowInCycle/focusHandoffId (Cmd-` front-to-back cycling, focus handoff on close/minimize); dockActivityBadges state-derived (amber approval chip, dream violet, artifact revision); windowMotion.js dock morph animations; threads registered in windowManager + Work group."
    extraction: "windows/shared.jsx consolidation (StatusLamp/MODEL_LANES/DEFAULT_MAPLE_MAX_TOKENS/statuses/displayText/formatTime); chat.css/artifact.css/windows.css started with transcript-floor work (>=160px transcript, rails yield under squeeze)."
  verification: "498/498 host (context_budget +5, quality +8) · 288/291 UI (3 skip) · 50/50 test_server.py · build clean · greedy equivalence IDENTICAL old-vs-new maple.py · checkpoint maple.py sha256 matches repo."
  known_gaps: "Decode gain partially credited to checkpoint-file sync (outside repo); MoE gather_qmm (7.25/11.6ms) is the next decode frontier — needs simdgroup-matrix kernels; live :8080 server runs pre-speed code until app restart; nothing committed."

mega-perf-reliability-impeccable-soak-eval-2026-09-22:
  date: "2026-09-22"
  rev: 8a5b0c8+dirty
  scope: "mega swarm: MoE fused decode kernels + durable-io/reliability + operate-mode impeccable hardening + real-main soak + tool-use eval expansion"
  perf:
    moe3: "Fused MoE decode kernels RETAINED — first real gather_qmm win. maple_moe_upgate (dequant in registers, packed uint4 x, 8-expert gate+up+SwiGLU in one dispatch) + maple_moe_down (weighted accumulation -> (1,1,2048)). Decode-gated; one-time _matches probe per layer falls back to gather_qmm. Live A/B :8081 8-rep medians: 72.4 -> 75.7 tok/s (+4.6%); fused_decode 244us vs 264-341us reference/layer; maxdiff <=0.03125 bf16, greedy identical. Receipts: .optimize/runs/moe-{before,after}-*.json."
    reverted: "maple_qgemv dequant-GEMV for attn qkv/o_proj (M=1): 1-row/simdgroup lost to qmm (58 vs 45us); 4-row x-amortized hit parity (~49 vs 47us) — never a win, reverted clean. Earlier scalar-dequant fused draft slower (15.09 vs 13.27ms) — reworked into retained version."
    note: "Session decode drifted (72 vs ~89 measured earlier) — thermal; A/B same-session is the arbiter. Checkpoint maple.py synced + backup .bak-moe3."
  reliability:
    durable_io: "durable_io.cjs — readJsonFile/writeJsonAtomic/appendJsonlLine shared helpers; corruption -> quarantine + integrity.recovered journal event + rebuild from .bak. context_broker state/journal + artifact registry now ride it."
    queue: "agent_queue: drain() try/catch — a receipted entry failure no longer propagates as unhandledRejection stranding the tail; submit() enqueues behind restored pending instead of leapfrogging; safePayload carries threadId/workspaceRoot/autonomy/fallbackLane (restored intents were silently retargeted to defaults); text cap 1000->4000."
    context_broker: "MAIN-THREAD FREEZE FIX: TCC-protected Skysight segments dir wedges open()/readdir uninterruptibly in-kernel on this host (session_restore + whole suite hung). Iterated: fs.promises pins a threadpool slot forever, AbortSignal can't fire, unref'd Worker blocks exit. Final: detached node -e probe child does the readdirSync scan -> stdout JSON; 3s Promise.race -> stop() (stdout.destroy+SIGKILL+unref); 5min backoff TTL; single in-flight dedup. Boot + suite now complete."
    caveat: "Wedged orphan probe PID 94184 unkillable in-kernel (harmless, unref'd, holds nothing); clears on reboot."
  soak:
    suite: "e2e_soak.test.cjs — 12 scenarios + 1 todo against real main.cjs under electron-mock/spawn-guard: intent flood, pause/resume drain, kill mid-task (honest task.blocked via allowlist-denied shell.exec), restart durability incl. FIFO-across-reboot, corrupt registry quarantine+rebuild, steering flood, approval approve/reject, budget boundary, batch mid-failure, cancel racing, failed-entry drain, drain-after-plan.reject."
    known: "Registry-corruption recovery is silent (no integrity.* event) — noted observability gap, contract verified."
  impeccable:
    operate: "Operate-mode hardening across src/**: duplicate-mutation guards, honest disabled/busy states, Escape+outside-click for all popovers, focus restoration, friendly error surfaces with exact-details disclosure, artifact title wrap, disabled-cursor normalization."
  eval:
    benchmark: "benchmark.json 11->27 tasks: 52-command registry snapshot, 9 pseudo-actions, 41 plannable; batch envelopes, deterministic-input failures, DECIDE_MIN_CONFIDENCE=0.35 + DECIDE_MIN_MARGIN=0.1 fixtures, channel-rejoin case."
    calibration: "tool_use_eval emits machine-readable calibration report -> evals/out/calibration-*.json."
  verification: "521/521 host (1 todo; 3 contention-timeout flakes pass in isolation) · 288/291 UI (3 skip) · 63/63 test_server.py · build clean · 8081 clean."
  known_gaps: "Unkillable wedged probe orphan until reboot; registry-rebuild is silent; soak's latestSegment comment is stale (fix landed); nothing committed."
